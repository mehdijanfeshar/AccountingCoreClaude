# آرشیو بکاپ — نسخهٔ پیش از بازآرایی مستندسازی (۲۰۲۶-۰۸-۲۸)

این پوشه صرفاً یک **بکاپ خواندنی** است، نه بخشی از جریان کاری فعال پروژه.

در ۲۰۲۶-۰۸-۲۸، `CLAUDE.md` (۷۵۳ خط) و `docs/progress-log.md` (۳۹۷ خط) بازآرایی شدند تا هزینهٔ توکنِ خواندنشان با هر فاز جدید بزرگ‌تر نشود — جزئیات به `docs/phase-log.md` و `docs/open-decisions.md` منتقل شد و خودِ این دو فایل کوتاه و فشرده شدند. محتوا **دوبار مستقل verify شد** (diff خط‌به‌خط برای `CLAUDE.md`، استخراج «اتم‌های واقعیت» برای `progress-log.md`) و هیچ چیزی عمداً حذف نشد.

با این حال، به‌درخواست صریح صاحب پروژه، نسخهٔ کامل **قبل از بازآرایی** هر دو فایل اینجا نگه داشته شده تا اگر بعداً معلوم شد چیزی از قلم افتاده، بشود مستقیم به این‌ها رجوع کرد — بدون نیاز به کندوکاو در تاریخچهٔ git.

- `CLAUDE.md.pre-restructure-2026-08-28.md` — دقیقاً همان `CLAUDE.md` قبل از commit بازآرایی (آخرین commit پیش از آن: `986b173`).
- `progress-log.md.pre-restructure-2026-08-28.md` — دقیقاً همان `docs/progress-log.md` قبل از همان commit.

⚠️ این دو فایل **بایگانی ثابت‌اند، به‌روزرسانی نمی‌شوند.** منبع حقیقتِ زندهٔ پروژه همیشه `CLAUDE.md`/`docs/phase-log.md`/`docs/open-decisions.md`/`docs/progress-log.md` فعلی است.
