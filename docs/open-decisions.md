# ریسک‌رجیستر و تصمیمات باز

این فایل **ریسک‌رجیستر زندهٔ** پروژه است — همهٔ تصمیمات باز، ریسک‌های شناخته‌شده، و مواردی که عمداً حدس زده نشدند. تا ۲۰۲۶-۰۸-۲۸ این محتوا داخل `CLAUDE.md` بود و بدون کم‌وکاست به اینجا منتقل شد.

`CLAUDE.md` فقط بخش کوتاه «ریسک‌های باز 🔴 (خلاصه)» را نگه می‌دارد که عنوان یک‌خطی موارد 🔴 را فهرست می‌کند؛ **جزئیات، دلایل، و همهٔ موارد 🟡 و ✅ حل‌شده فقط اینجا هستند.**

⚠️ **این فایل زنده است** — هر فاز جدید باید موارد جدیدش را اینجا اضافه کند و موارد حل‌شده را با ~~خط‌خورده~~ + ✅ علامت بزند (پاک نکند؛ سابقهٔ تصمیم است). هر بار که یک مورد 🔴 اضافه یا حل شد، بخش خلاصهٔ `CLAUDE.md` هم باید هم‌زمان به‌روز شود.

⚠️ **دربارهٔ ارجاع‌های داخلی:** متن زیر عیناً منتقل شده. ارجاع‌هایی مثل «فاز ۹/۱۰/۱۱ بالاتر/پایین‌تر» حالا به `docs/phase-log.md` اشاره می‌کنند؛ «تصمیم معماری دوم بالا» همچنان در `CLAUDE.md` است.

---

## تصمیمات باز (برای فاز بعدی)

### ✅ حل‌شده در ۲۰۲۶-۰۸-۱۷

- **مدل نوشتنِ معتبر** — کاربر گزینهٔ «ج» را انتخاب کرد: Legacy جایگزین کامل. رجوع به «تصمیم معماری دوم» بالا.
- **ابهام `TB_ACCOUNT_LINK_TAFSILI`** — حل شد. `TB_ACCOUNT_LINK_TAFSILGROUP` منبع حقیقت است (FK `FK_TAFSILGOUP_ACCOUNTCODE` به `TB_ACCOUNTCODE` + UNIQUE `UK_ACCOUNTLINKTAFSILGROUP` روی `ACCOUNT_ID, LEVEL_ID, TAFSILGROUP_ID`). `TB_ACCOUNT_LINK_TAFSILI` علی‌رغم نامش به `TB_ACCOUNT` (حساب **بانکی**) وصل است و به کدینگ ربطی ندارد — احتمالاً برای پیش‌فرض تفصیلی در تراکنش‌های بانکی/چک. `TB_ACCOUNT_LINK_LEVEL` فقط سطح را فعال می‌کند (ستون `TAFSILGROUP_ID` ندارد) و به‌تنهایی منبع حقیقت نیست.

### تصمیمات باز

- **✅ ~~مرز Aggregate برای Head/Detail~~ — حل شد (۲۰۲۶-۰۸-۲۰، تصمیم صریح صاحب پروژه).** در پروژهٔ خارجی `Tamin.Core`، هیچ‌کدام از ۶ رابطهٔ Head→Detail کپسوله نبودند (`VouchersDetail` به‌عنوان Aggregate Root مستقل مدل شده بود)، که با cascade soft-delete فاز ۹ ما (`TB_VOUCHERSHEAD → TB_VOUCHERSDETAIL → TB_VOUCHERDETAIL_LINK_TAFSILI`) در تضاد بود. **تصمیم نهایی — مدل ترکیبی، نه یکی از آن دو حالت:**
  - `TB_VOUCHERSDETAIL` **مستقل** است و CRUD خودش را می‌گیرد (`CreateVoucherDetailCommand`/`UpdateVoucherDetailCommand`/`DeleteVoucherDetailCommand`) — قابل‌فراخوانی مستقل برای افزودن/ویرایش دیتیل به سند **موجود**.
  - **اما** ثبت اولیهٔ سند باید هدر+دیتیل(ها) را **با هم، در یک تراکنش** ذخیره کند — یعنی `CreateVoucherHeadCommand` باید توانایی گرفتن یک لیست دیتیل اولیه را هم داشته باشد (composite create).
  - **cascade soft-delete فاز ۹ دست‌نخورده و همچنان معتبر است** — این تصمیم فقط دربارهٔ Create/Update است، نه Delete.
  - جزئیات کامل در `docs/tamin-core-entity-reference.md` بخش ۵ (بلوک «✅ حل شد»).
  - **✅ پیاده‌سازی شد در «فاز ۱۰» (۲۰۲۶-۰۸-۲۰)** — رجوع به بخش «فاز ۱۰» بالاتر برای Endpointها، تصمیم composite-create (گزینهٔ الف) و دلایلش. این تصمیم دیگر فقط ثبت‌شده نیست؛ در کد زنده است و با ۱۰۷ تست پوشش دارد.
  - **همچنین تثبیت شد:** هر جدولی که نامش با `_LINK_TAFSIL*`/`_LINK_LEVEL` تمام می‌شود (هر ۱۰ مورد — فهرست کامل در سند مرجع) **همیشه تعبیه‌شده می‌ماند**، هیچ‌وقت CRUD مستقل نمی‌گیرد. این قاعدهٔ کلی تیم است، نه فقط برای `VouchersDetailLinkTafsili`.
- **🟡 دو Entity مبهم بدون طبقه‌بندی قطعی** (`TB_RABET_CLOSING`, `TB_CHARGE_LINK_COST`) — در `Tamin.Core` سیگنال کپسولگی‌شان نیمه‌کاره/متروک است (بخش ۴ سند مرجع). قبل از ساخت CRUD برایشان از کاربر بپرس.
- **🟡 نوع دادهٔ مبلغ: `long` در برابر `decimal?`** — در سراسر `Tamin.Core` مبالغ (`Debtor`/`Creditor` و مشابه در ۶ Entity) از نوع `long` غیرnullable‌اند، در حالی‌که ما `decimal?` مدل کرده‌ایم. یا واحد پول ریال بدون اعشار ذخیره می‌شده، یا ما داریم دقتی اضافه می‌کنیم که Legacy ندارد. قبل از ساخت Command روی سند باید روشن شود.
- **🔴 «الزامی بودن تفصیلی» در Legacy اصلاً مدل شده یا نه؟** در `TB_ACCOUNT_LINK_TAFSILGROUP`, `TB_TAFSIL_GROUP`, `TB_LEVEL_TAFSIL`, `TB_ACCOUNT_LINK_LEVEL` هیچ ستون معادل `DetailRequirement` (الزامی/اختیاری) پیدا نشد. برای قطعی‌شدن نیاز است: (۱) جست‌وجوی ستون‌هایی با نام `MUST`/`ISREQUIRED`/`OBLIGATORY` در سایر جدول‌ها، (۲) کوئری روی دادهٔ واقعی برای فهمیدن اینکه آیا این قاعده در Application/UI سیستم قدیمی enforce می‌شده. **حدس زده نشد.**
- **🟡 نقش واقعی `TB_ACCOUNT_LINK_LEVEL`** — مشخص نیست فعال است یا artifact قدیمی. نیاز به کوئری روی داده (تعداد ردیف‌های `ISDELETED=0` و هم‌پوشانی با `TB_ACCOUNT_LINK_TAFSILGROUP`).
- **🟡 یکپارچگی ارجاعی تفصیلی در سند** — `TB_VOUCHERDETAIL_LINK_TAFSILI` فقط **یک** FK دارد (به `TB_VOUCHERSDETAIL`)؛ `TAFSILI_ID` و `LEVEL_ID` هیچ FK ای ندارند. یعنی در سطح دیتابیس هیچ تضمینی نیست که تفصیلی ثبت‌شده روی ردیف سند اصلاً وجود داشته باشد یا مجاز باشد. اگر این تضمین لازم است باید صریحاً ساخته شود.
- **🟡 تضمین تراز (Debit == Credit)** — با تصمیم دوم کاربر از سطح کد حذف شد. اگر لازم شد باید در لایهٔ Application یا به‌صورت DB constraint بازسازی شود.
- **~~نگاشت `VoucherLine.DetailPolicySnapshot`~~** — منتفی شد؛ `VoucherLine` منسوخ است و مسیر نوشتن دیگر از آن عبور نمی‌کند.
- **`AccountNature` فعلاً فقط برچسب گزارشی است** و در اعتبارسنجی دخالت ندارد؛ اگر قرار است مانده بر اساس ماهیت محاسبه شود، جای آن سمت Read (View/MV) است.
- **~~حذف فیزیکی کد منسوخ~~** — ✅ انجام شد (۲۰۲۶-۰۸-۱۷، به درخواست صریح کاربر پس از init شدن git). ۲۲ فایل حذف شد؛ در commit `9f760ad` قابل بازیابی است.
- **🔴 DEFAULTهای سمت Oracle که با `Guid` سازگار نیستند** (کشف‌شده ۲۰۲۶-۰۸-۱۸ حین تبدیل string→Guid). ۹ ستون DEFAULT دارند که مقدار تولیدی‌شان GUID معتبر نیست و خواندنش با converter فعلی `FormatException` می‌دهد:
  - `TB_ACCOUNTCODE.PARENTID` با `DEFAULT '0'` — `'0'` یک GUID نیست. احتمالاً در سیستم قدیمی sentinel «بدون والد» برای گره ریشه بوده؛ ولی در دادهٔ فعلی هیچ ردیفی این مقدار را ندارد (ریشه‌ها `NULL` دارند).
  - ۸ جدول با `ID DEFAULT sys_guid()`: `TB_CITY`, `TB_PROVINCE`, `TB_RABET`, `TB_RABET_CLOSING`, `TB_VAHED_INFO`, `TB_VAHED_TYPE`, `TB_WHITEANDBLACKLIST`, `TB_WHITELIST`. تابع `sys_guid()` مقدار `RAW(16)` برمی‌گرداند که داخل `CHAR(36)` به‌صورت **۳۲ کاراکتر بدون dash و UPPERCASE** (فرمت `"N"`) نوشته می‌شود و `Guid.ParseExact(s,"D")` آن را رد می‌کند.
  - **وضعیت فعلی بی‌خطر است** چون اپلیکیشن قدیمی همیشه `ID` را صریح می‌داده و در دادهٔ زنده هیچ نمونه‌ای یافت نشد. خطر برای **کد جدید** ماست: هر `INSERT` که این ستون‌ها را خالی بگذارد، ردیفی می‌سازد که خواندن بعدی‌اش crash می‌کند.
  - **تصمیم آگاهانه:** خواندن سخت‌گیرانه (`ParseExact`) نگه داشته شد تا خطا **بلند** شود؛ جایگزین یعنی `Guid.Parse` سهل‌گیر، مقدار `"N"` را می‌پذیرد و در نوشتن بعدی بی‌صدا به فرمت dashed بازنویسی می‌کند — یعنی تغییر خاموش دادهٔ Legacy، که برای سیستم حسابداری بدتر از crash است.
  - **قبل از اولین Command/Query که این ۹ ستون را لمس کند باید یکی انتخاب شود:** (الف) همیشه ID را در کد Application تولید کنیم و هرگز به DEFAULT تکیه نکنیم، (ب) DEFAULTها با DDL اصلاح شوند، (ج) مسیر خواندن defensive شود.
- **✅ ~~پوشش تست مدل نوشتن جدید صفر است~~** — حل شد در ۲۰۲۶-۰۸-۱۸. با فاز ۵، پروژهٔ `Accounting.Application.Tests` با ۴۱ تست ساخته شد (مجموع ۶۳). ⚠️ ولی این تست‌ها **همگی Unit با Mock** هستند؛ هیچ تست integration روی Oracle واقعی وجود ندارد، پس صحت Fluent Mapping و رفتار واقعی INSERT هنوز اثبات‌نشده است.

- **✅ ~~`TB_ACCOUNTCODE.PARENTID` با `DEFAULT '0'` مسیر ساخت حساب ریشه را خراب می‌کند~~** — حل شد (۲۰۲۶-۰۸-۱۸). `HasDefaultValueSql("'0'")` از Fluent Mapping `PARENTID` حذف شد؛ فقط `HasConversion(GuidToChar36Converter)` باقی ماند. تأیید کاربر بر اساس قاعدهٔ کدینگ Legacy: گروه بدون والد (`PARENTID = null`)، کل←گروه، معین←کل (مثال عددی: گروه `11` → کل `1101` → معین `110101`)، پس این ستون باید بتواند واقعاً `NULL` باشد. حالا که `HasDefaultValueSql` حذف شده، EF Core دیگر `PARENTID` را `ValueGenerated.OnAdd` نمی‌داند و همیشه مقدار CLR واقعی (شامل `null` برای حساب ریشه) را صریح در INSERT می‌فرستد — DDL خود Oracle دست‌نخورده ماند. build ۰ خطا، ۶۳/۶۳ تست سبز. توجه: `TB_VOUCHERSHEAD.DOCLIFE` (`DEFAULT 0`) و `HEAD_DESC` (`DEFAULT '-'`) همچنان دست‌نخورده‌اند چون بی‌خطرند (مقدارشان با نوع ستون سازگار است). ۸ جدول دیگر با `sys_guid()` (خط بالا) هنوز حل‌نشده باقی‌اند — هنوز هیچ Command به آن‌ها دست نزده.

- **🟡 دو UserSecrets store رقیب با همان کلید** — `Accounting.Api.csproj` دارای `UserSecretsId = f5497a90-...` و `Accounting.Infrastructure.csproj` دارای `99fb4522-...` است و **هر دو** کلید `ConnectionStrings:DefaultConnection` را دارند. `Program.cs` علاوه بر بارگذاری خودکار store پروژهٔ Api، صراحتاً `AddUserSecrets<Accounting.Infrastructure.AssemblyMarker>()` را هم صدا می‌زند؛ چون این provider **بعداً** اضافه می‌شود، مقدار store مربوط به Infrastructure برنده است. الان مقدار هر دو یکسان است پس مشکلی بروز نمی‌کند، ولی اگر کسی فقط یکی را عوض کند، تغییرش ممکن است بی‌صدا نادیده گرفته شود. پیشنهاد: یکی از دو store حذف شود.

- **✅ ~~خطای UNIQUE به‌صورت خام به بیرون درز می‌کند~~** — حل شد در ۲۰۲۶-۰۸-۱۸ (فاز ۶). `UK_ACCOUNTCODE` و `UK_VOUCHERHEAD_NUMBER` حالا از طریق `UnitOfWork.SaveChangesAsync` (تشخیص `OracleException { Number: 1 }`) به `DuplicateKeyException` و سپس به **409 Conflict** با پیام عمومی نگاشت می‌شوند. ⚠️ این نگاشت فقط با Unit Test (ساخت `OracleException` واقعی از طریق reflection روی constructor داخلی درایور) اثبات شده؛ **روی Oracle واقعی هرگز اجرا نشده**. ضمناً بررسی پیش‌دستانه (pre-check) وجود ندارد — اتکا کاملاً به constraint سطح DB است، که برای شرایط رقابتی (race) در واقع رفتار درستی است.

### 🔴 تصمیمات باز جدید — امنیت (کشف‌شده ۲۰۲۶-۰۸-۱۸ توسط `security-reviewer` در Gate فاز ۶)

با ساخته‌شدن اولین سطح HTTP، این موارد از حالت نظری خارج شدند و **قبل از هرگونه میزبانی خارج از localhost باید تصمیم‌گیری شوند**:

- **✅ ~~🔴 CRITICAL — هیچ Authentication/Authorization ای وجود ندارد.~~** — **حل شد در ۲۰۲۶-۰۸-۱۹ (فاز ۷).** IDP واقعی سازمان (`Tamin.Framework.Common.Security`) + `app.UseAuthentication()` (که اصلاً در pipeline نبود) + `SetFallbackPolicy(RequireAuthenticatedUser)` + `[AllowAnonymous]` فقط روی `HealthController`. جزئیات کامل و تصمیم باز باقی‌مانده (نگاشت claim) پایین‌تر.
- **✅ ~~🔴 CRITICAL — `ADDUSERID` توسط خود فراخوان قابل جعل است.~~** — **حل شد در ۲۰۲۶-۰۸-۱۹ (فاز ۷).** `AddUserId` کاملاً از هر دو Command حذف شد و مقدار از `ICurrentUser.UserId` سمت سرور می‌آید. چون پراپرتی دیگر **اصلاً وجود ندارد**، بازگشت این آسیب‌پذیری خطای کامپایل می‌دهد نه خطای منطقی (تأیید `qa-tester`).

### ✅ ~~تصمیم باز — اتصال inbound به IDP واقعی سازمان~~ — حل شد (۲۰۲۶-۰۸-۱۹)

صاحب پروژه پکیج رسمی `Tamin.Framework.Common.Security` 1.0.9 را در اختیار گذاشت و مسیر inbound به IDP واقعی سازمان وصل شد (فاز ۷ بخش ۳). سؤال‌های قبلی به‌این‌ترتیب منتفی شدند: پکیج خودش `ValidIssuer = http://idm.tamin.ir` و یک `JsonWebKey` ثابتِ جاسازی‌شده دارد، پس **نه discovery URL لازم است نه JWKS fetch**. audience هم به‌صراحت درخواست کاربر فعلاً با `Financial_Account` مشترک است.

پکیج `IDP` (که در کد outbound `TokenManager` استفاده شده بود) روی feed سازمان **پیدا نشد** → یعنی یک پروژهٔ لوکال داخل solution اصلی کاربر بوده، نه پکیج منتشرشده. پس پورت دستی `TokenManager` به `Accounting.Infrastructure/Idp/` تصمیم درستی بوده و باقی می‌ماند.

### 🔴 تصمیم باز جدید — نگاشت claim برای `ADDUSERID` (۲۰۲۶-۰۸-۱۹)

**این تنها سؤال باقی‌مانده از بستهٔ auth است و عمداً حدس زده نشد.**

`HttpContextCurrentUser.UserId` فعلاً `ClaimTypes.NameIdentifier` را می‌خواند و اگر غایب یا **بیش از ۱۰ کاراکتر** باشد throw می‌کند (عرض ستون Legacy `ADDUSERID`). اما:

- **معلوم نیست کدام claim در توکن واقعی IDP سازمان یک شناسهٔ ≤۱۰ کاراکتری قابل‌نگاشت به `ADDUSERID` دارد.** اگر `sub` یک GUID باشد، قطعاً جا نمی‌شود.
- نکتهٔ مرتبط: پکیج `NameClaimType` را روی `.../identity/claims/name` ست می‌کند (نه `nameidentifier`)، پس `User.Identity.Name` به claim دیگری اشاره می‌کند.
- معلوم نیست بین کدهای کاربری قدیمی Legacy (که ستون `ADDUSERID` را پر کرده‌اند) و هویت‌های IDP جدید اصلاً نگاشتی وجود دارد یا نه.

**⚠️ این هرگز با یک توکن واقعی IDP تست/تأیید نشده** (هیچ فراخوان شبکه‌ای به IDP انجام نشد).

**چرا فعلاً بی‌خطر است:** طراحی `ICurrentUser` عمداً **fail-loud** است — هر ناسازگاری بلافاصله خطا می‌دهد، نه اینکه بی‌صدا یک مقدار غلط/بریده در ستون Audit یک سیستم حسابداری بنویسد. این رفتار **درست است و باید حفظ شود**؛ فقط باید با اولین توکن واقعی راستی‌آزمایی شود.

### 🟡 تصمیمات باز دیگر که در فاز ۷ عمداً حل نشدند

- **`VahedCode` سمت سرور اعمال نمی‌شود.** `ICurrentUser.VahedCode` ساخته شد ولی **به هیچ فیلتر Query وصل نشد**. یعنی `?vahedCode=` همچنان یک پارامتر اختیاری و غیرقابل‌اعتماد است و هر کاربر احراز‌شده می‌تواند اسناد هر واحد سازمانی دیگری را ببیند. باید تصمیم گرفته شود: اجباری و سمت‌سروری شود، یا دید بین‌واحدی صریحاً عمدی اعلام شود.
- **IDOR روی `GetById`** — فاز ۷ فقط authentication و جعل Audit را حل کرد، **نه authorization در سطح رکورد**. هر کاربر احراز‌شده می‌تواند هر `ID` ای را بخواند. باید تأیید شود که این برای این مرحله قابل‌قبول است.
- **محدودکردن Kestrel به `localhost`** — توصیهٔ `security-reviewer` به‌عنوان کاهش‌دهندهٔ ریسک فوری و بدون کد، تا وقتی auth نهایی مستقر نشده. تصمیم عملیاتی با صاحب پروژه.
- **پوشش تست pipeline احراز هویت** — تست‌های `Accounting.Api.Tests` همگی unit اند (بدون `WebApplicationFactory`)، پس این‌که واقعاً یک درخواست بدون توکن ۴۰۱ می‌گیرد و `FallbackPolicy` عملاً اعمال می‌شود **در هیچ تستی اثبات نشده**. `qa-tester` این را به‌عنوان بزرگ‌ترین شکاف پوشش اعلام کرد و عمداً نبست، چون `AddInfrastructure` هنگام ساخت host یک `UseOracle` ثبت می‌کند و یک تست ساده‌لوحانه ممکن بود ناخواسته به دیتابیس **زندهٔ** واقعی وصل شود.
- **🟡 چندمستأجری (`VAHEDCODE`) مرز ایزولاسیون نیست.** `vahedCode` یک پارامتر **اختیاری query string** است که فراخوان خودش می‌دهد، نه فیلتری که سرور از هویت استخراج کند؛ یعنی هر کسی می‌تواند اسناد هر واحد سازمانی دیگری را مرور کند. باید صریحاً تصمیم گرفته شود: یا اجباری و سمت‌سروری شود، یا مستند شود که دید بین‌واحدی عمدی است.
- **🟡 فیلدهای Audit در DTO برای همه قابل خواندن‌اند** — `AddUserId`/`ChangeUserId`/`IsDeleted` در هر دو DTO بی‌قید برگردانده می‌شوند. پس از افزودن نقش‌ها باید تصمیم گرفته شود که آیا این‌ها نیاز به نقش ممتاز دارند.
- **🟡 هیچ Rate Limiting ای وجود ندارد** — سقف ۲۰۰ ردیف در هر صفحه کار می‌کند، ولی هیچ محدودیتی روی تعداد درخواست نیست، پس پیمایش کامل جدول‌ها با اسکریپت ممکن است.
- **🟡 بدنهٔ POST همان MediatR Command است** — هر فیلدی که بعداً به Command اضافه شود، بی‌صدا بخشی از قرارداد عمومی API می‌شود. پیشنهاد `api-contract`: افزودن `CreateXRequest` نازک در لایهٔ Api. (اجرا نشد.)
- **🟡 نسخه‌بندی API وجود ندارد** (`/api/...` بدون `/v1`). افزودن آن بعداً خودش یک breaking change است.

### 🔴 تصمیمات باز جدید — کشف‌شده در فاز ۸ (۲۰۲۶-۰۸-۱۹)

این‌ها **عمداً حل نشدند** چون هرکدام یک تصمیم کسب‌وکاری‌اند، نه یک نقص پیاده‌سازی. طبق Accounting Safety Gate بی‌صدا رد نشدند:

- **🔴 IDOR حالا به مسیر نوشتن هم رسید — تشدید ریسک، نه صرفاً انتقال آن.** تا پیش از این فاز، نبودِ authorization در سطح رکورد فقط روی `GetById` بود یعنی **فقط خواندن**. حالا هر کاربر احراز‌شده می‌تواند **هر** `TB_ACCOUNTCODE` یا `TB_VOUCHERSHEAD` را با دانستن `ID` آن **ویرایش یا حذف کند** — از جمله رکوردهای واحدهای سازمانی دیگر، چون `VAHEDCODE` هنوز سمت سرور اعمال نمی‌شود. این فاز عمداً هیچ قانون IDOR جدیدی اختراع نکرد (طبق دستور صریح)، ولی شدت ریسک موجود از «افشای اطلاعات» به **«تغییر/حذف غیرمجاز دادهٔ حسابداری»** ارتقا یافت. **پیش از هر استقراری باید تصمیم‌گیری شود.**
- **✅ ~~حذف سند هیچ cascade ای ندارد~~** — **کاملاً حل شد (۲۰۲۶-۰۸-۲۰، فاز ۹).** `DeleteVoucherHeadCommand` حالا cascade **کامل سه‌سطحی** دارد: `TB_VOUCHERSHEAD` → `TB_VOUCHERSDETAIL` → `TB_VOUCHERDETAIL_LINK_TAFSILI`، هر سه سطح در **یک تراکنش/یک `SaveChangesAsync`**. رجوع به «فاز ۹» پایین‌تر.
- **🟡 سندی که از قبل حذف شده ولی زیرمجموعهٔ فعال دارد، هرگز تمیز نمی‌شود.** پیامد ظریفِ گارد idempotency: چون `if (entity.ISDELETED == true) return;` بالای cascade است، اگر سندی **پیش از فاز ۹** حذف شده باشد (یعنی وقتی هنوز cascade وجود نداشت)، ردیف‌ها و لینک‌های تفصیلی‌اش همچنان `ISDELETED = false` مانده‌اند و فراخوانی دوبارهٔ Delete هم آن‌ها را تمیز **نمی‌کند**. اگر دادهٔ Legacy چنین اسنادی دارد، به یک اسکریپت backfill یک‌بارمصرف نیاز است — **بررسی نشد و حدس زده نشد** (نیازمند کوئری روی دادهٔ زنده).
- **🔴 حذف گرهٔ کدینگ هیچ بررسی وابستگی ندارد.** `DeleteAccountCodeCommand` یک گره را soft-delete می‌کند بدون اینکه بررسی کند (الف) آیا فرزندی دارد (`PARENTID` خودارجاع، `FK_SELFREFRENCE`) یا (ب) آیا در ردیف‌های سند موجود استفاده شده. پس می‌توان یک گروه را حذف کرد و فرزندان یتیم ولی فعال باقی بمانند. حذف فیزیکی نیست پس FK دیتابیس شکایتی نمی‌کند — یعنی این ناسازگاری **کاملاً بی‌صدا** است.
- **🔴 سند Post شده حالا واقعاً قابل تغییر است.** `UpdateVoucherHeadCommand` اجازه می‌دهد `DOCLIFE` (وضعیت سند) آزادانه عوض شود. invariant «تغییرناپذیری سند پس از Post» در تصمیم دوم آگاهانه حذف شده بود، ولی تا پیش از این فاز مسیر نوشتن فقط `INSERT` داشت پس عملاً قابل بهره‌برداری نبود. **حالا یک مسیر واقعی و در دسترس برای ویرایش/برگرداندن سند نهایی‌شده وجود دارد.** اگر این تضمین لازم است، باید صریحاً در Application یا به‌صورت DB constraint بازسازی شود.
- **🟡 هیچ کنترل همزمانی (optimistic concurrency) وجود ندارد.** schema Legacy ستون `rowversion`/`ETag` ندارد و هیچ‌کدام از دو PUT جدید توکن همزمانی نمی‌گیرند، پس رفتار **last-write-wins** است: دو ویرایش هم‌زمان روی یک سند، یکی را بی‌صدا از بین می‌برد. برای سیستم حسابداری باید آگاهانه پذیرفته یا حل شود (مثلاً مقایسهٔ `UPDATEDDATE` به‌عنوان توکن، یا `If-Match`).
- **🟡 هیچ مسیر undelete/restore وجود ندارد.** چون Update رکورد soft-deleted را 404 می‌دهد و `ISDELETED` در Update نیست، رکورد حذف‌شده از طریق API **قابل بازگردانی نیست**. اگر بازگردانی لازم است، به یک Command صریح (`RestoreXCommand`) نیاز دارد — عمداً در این فاز ساخته نشد.

### 🔴 تصمیمات باز جدید — کشف‌شده در فاز ۱۰ (۲۰۲۶-۰۸-۲۰)

طبق Accounting Safety Gate، این‌ها **بی‌صدا رد نشدند** و هیچ‌کدام حدس زده نشد:

- **🔴 حالا برای اولین بار یک عملیات واحد وجود دارد که می‌توانست تراز را تضمین کند — و نمی‌کند.** پیش از این فاز، سرسند و ردیف‌ها هرگز در یک درخواست ساخته نمی‌شدند، پس «بدهکار = بستانکار» عملاً قابل بررسی نبود. با composite create، کل سند (هدر + همهٔ ردیف‌ها) در **یک تراکنش** می‌آید — یعنی تنها نقطه‌ای که چنین تضمینی طبیعتاً جا می‌گیرد، حالا وجود دارد. طبق «تصمیم معماری دوم» عمداً پیاده **نشد**. اگر لازم است، باید صریحاً به‌صورت validation در `CreateVoucherHeadCommandValidator` یا DB constraint بازسازی شود. ضمناً هیچ چیز مانع نمی‌شود که یک ردیف هم‌زمان `DEBTOR` و `CREDITOR` غیرصفر داشته باشد.
- **🔴 نوع دادهٔ مبلغ هنوز حل نشده و حالا در مسیر نوشتن است.** تصمیم باز 🟡 «`long` در برابر `decimal?`» (از سند مرجع `Tamin.Core`) تا این فاز نظری بود؛ حالا `DEBTOR`/`CREDITOR` واقعاً از API پذیرفته و نوشته می‌شوند. فعلاً `decimal?` نگه داشته شد چون با Fluent Mapping فعلی (`NUMBER(25)`) می‌خواند، ولی **تأیید نشده** که Legacy اعشار را می‌پذیرد یا ریال صحیح ذخیره می‌کند. **قبل از هر داده‌ای که واقعاً روی Oracle نوشته شود باید روشن شود.**
- **✅ ~~🔴 نقض FK به‌جز UNIQUE هیچ نگاشتی ندارد → 500 خام~~ — حل شد (۲۰۲۶-۰۸-۲۵، فاز ۱۱).** گزینهٔ «نگاشت مرکزی در `UnitOfWork`» انتخاب شد (نه pre-check صریح، تا قانون کسب‌وکاری اختراع نشود و شرایط رقابتی هم درست مدیریت شود — همان قضاوتی که قبلاً برای UNIQUE اعمال شده بود). ORA-02291 → `ForeignKeyViolationException` → **400 Bad Request** با پیام عمومی. جزئیات و دلیل انتخاب 400 به‌جای 409/404 در «فاز ۱۱» بالاتر. ⚠️ **زیرمجموعه‌ای که همچنان باز است:** `TB_VOUCHERDETAIL_LINK_TAFSILI.TAFSILI_ID`/`LEVEL_ID` اصلاً FK ندارند، پس تفصیلی نامعتبر بی‌صدا نوشته می‌شود و این نگاشت کمکی نمی‌کند (رجوع به مورد «یکپارچگی ارجاعی تفصیلی در سند» بالاتر).
- **🟡 عدم تقارن `VahedCode`/`Year` بین دو مسیر ساخت.** در composite create از سرسند مشتق می‌شوند؛ در `CreateVoucherDetailCommand` مستقل، فراخوان خودش می‌دهد و **هیچ اعتبارسنجی متقاطعی با سرسند انجام نمی‌شود**. یعنی می‌توان ردیفی با `YEAR` متفاوت از سند والدش ساخت. آیا باید مشتق/اعتبارسنجی شود؟
- **🟡 `RADIF` (شمارهٔ ردیف) هیچ مدیریتی ندارد** — نه تولید خودکار، نه تضمین یکتایی در محدودهٔ یک سند، نه پیوستگی. کاملاً ورودی فراخوان است. مرتب‌سازی خواندن `OrderBy(RADIF).ThenBy(ID)` است و چون `int?` است، `NULL`ها در Oracle آخر می‌آیند.
- **✅ ~~🟡 هیچ مسیر نوشتنی برای تفصیلیِ ردیف سند وجود ندارد~~ — حل شد (۲۰۲۶-۰۸-۲۵، فاز ۱۱).** گزینهٔ «لیست تودرتو روی `CreateVoucherDetailCommand`» انتخاب و به `UpdateVoucherDetailCommand` هم تعمیم داده شد (`TafsiliLinks`، پارامتر پایانیِ اختیاری، غیر‌breaking). قاعدهٔ تیمی «تعبیه‌شده برای همیشه» دست‌نخورده ماند — نه Controller، نه MediatR request، نه repository اختصاصی؛ متدها parent-scoped روی `IVoucherDetailRepository` اند و گارد `NoIndependentLinkTableWritePathTests` **گسترش یافت** (نه دور زده شد). رفتار Update = جایگزینی کامل، با `null` = «دست نزن» و `[]` = «همه را پاک کن». جزئیات کامل و دلایل در «فاز ۱۱» بالاتر.
  - ⚠️ **دو شکاف باقی‌مانده که عمداً حدس زده نشد:** (۱) **سمت خواندن هنوز تفصیلی را برنمی‌گرداند** — `VoucherDetailDto` فیلد لینک ندارد، پس فراخوان نمی‌تواند لینک‌های فعلی یک ردیف را ببیند (دقیقاً همین محدودیت، دلیل تفکیک `null` از `[]` شد). برای «فرم صدور سند با تفصیلی داینامیک» این لازم خواهد بود. (۲) **مسیر composite create سند (`CreateVoucherHeadCommand.InitialDetails`) هنوز تفصیلی نمی‌پذیرد** — یعنی ثبت یک‌مرحله‌ای سند کامل با تفصیلی هنوز ممکن نیست و باید در دو مرحله انجام شود. هر دو خارج از دامنهٔ صریح فاز ۱۱ بودند.
- **🟡 تکرار منطق cascade در دو repository.** `IVoucherHeadRepository.SoftDeleteDetailTreeAsync` (سطح ۲+۳) و `IVoucherDetailRepository.SoftDeleteTafsiliLinksAsync` (سطح ۳) حالا هم‌پوشانی دارند. به دستور صریح کاربر فاز ۹ دست‌نخورده ماند، ولی XML doc خودِ فاز ۹ گفته بود در این حالت باید بازبینی/استخراج شود. **follow-up ثبت‌شده، انجام نشده.**
- **🟡 (یافتهٔ `/code-review`) race شرطی روی وجود سرسند → 500 خام به‌جای 404.** بررسی «آیا سرسند وجود دارد/حذف نشده» در `CreateVoucherDetailCommandHandler` از نوع check-then-act است، نه تراکنشی. اگر بین این چک و `SaveChangesAsync` یک `DeleteVoucherHeadCommand` هم‌زمان روی همون سرسند commit بشه، insert روی FK ناموجود می‌خوره و به‌جای 404 تمیز، 500 خام از `GlobalExceptionHandler` برمی‌گرده. هم‌راستا با تصمیم قبلی پروژه (race روی UNIQUE هم عمداً به constraint سطح DB سپرده شده)، **عمداً حل نشد**.
- **🔴 IDOR حالا به ردیف سند هم رسید.** همهٔ ریسک‌های authorization فاز ۷/۸ عیناً روی ۵ Endpoint جدید هم برقرارند: هر کاربر احراز‌شده می‌تواند ردیف هر سندی از هر واحد سازمانی را بخواند/بسازد/ویرایش/حذف کند. سطح حمله بزرگ‌تر شد، ماهیت ریسک تغییر نکرد. **مسدودکنندهٔ استقرار.**

### 🔴 تصمیمات باز جدید — کشف‌شده در فاز ۱۲ (۲۰۲۶-۰۸-۲۵/۲۶)

- **🔴 دو باگ `bool?`→enum تأییدشده در مسیر نوشتن فعال، هنوز اصلاح نشده‌اند.** `TB_ACCOUNTCODE.TYPECODE` (باید `Group=1/Kol=2/Moin=3` باشد) و `TB_VOUCHERSHEAD.DOCLIFE` (باید ۴مقداری باشد). هر دو با شواهد مستقل (کد پروژهٔ مرجع + امروز با کوئری زندهٔ Oracle برای `TYPEACTIVITY`) تأیید شدند. **قبل از اصلاح باید تصمیم گرفت:** enum واقعی C#؟ `byte?`؟ آیا نیاز به migration روی دادهٔ موجود هست (احتمالاً نه، چون فقط نوع CLR عوض می‌شود، نه ستون فیزیکی Oracle)؟ رجوع «فاز ۱۲» بالاتر برای جزئیات کامل.
- **🟡 ۱۷ ستون دیگر با همین الگوی مشکوک، هنوز اسکن نشده‌اند.** فقط ۴ ستون روی `TB_ACCOUNTCODE`/`TB_VOUCHERSHEAD` عمیق بررسی شد. بقیهٔ ۱۳ جدول (شامل `TB_TAFSILI.ISACTIVE` که خطرناک‌ترین مورد شناسایی‌شده است) هنوز نه اسکن شدند نه اصلاح. پیشنهاد Team-lead سابق: قبل از ساخت CRUD روی هر Entity جدید، اول چک شود آیا این الگو رویش صدق می‌کند.
- **🔴 دادهٔ Legacy موجود از قبل با یک قاعدهٔ محتمل در تناقض است.** ۳ حساب سطح گروه واقعی `TYPEACTIVITY ∈ {4,5,6}` دارند، در حالی که پروژهٔ مرجع برای سطح گروه فقط ۱..۳ را مجاز می‌داند. اگر بخواهیم این Validator را پیاده کنیم، باید تصمیم بگیریم با این ۳ رکورد تاریخی چه کنیم (نادیده گرفتن؟ استثنا؟ اصلاح داده؟) — **حدس زده نشد.**
  - **تصحیح دامنه (۲۰۲۶-۰۹-۰۶، فاز ۱۷):** قاعده در پروژهٔ مرجع **سطح‌محور** است، نه یکسان: `AddGroupCodeValidator` مقدار **۱..۳** را مجاز می‌داند ولی `AddMoinCodeCommandValidator` مقدار **۱..۷** را. پس تناقض **فقط روی سطح گروه** است و پابرجا می‌ماند؛ ولی دامنه‌اش دقیق‌تر شد و نشان می‌دهد `TYPEACTIVITY` ذاتاً هفت‌مقداری است. رجوع `docs/centralaccount-business-reference.md` §۲۴-۱۱.
- **✅ ~~🟡 عرض/معنای `VAHEDTYPE` هنوز قطعی نیست~~ — شاهد قوی پیدا شد (۲۰۲۶-۰۹-۰۶، فاز ۱۷).** دلیل شکست تلاش قبلی این بود که **enum اشتباهی مقایسه می‌شد**: در `Tamin.Core`، `Tafsili.VahedType` از نوع **`TypeKoli`** است (۱=بیمه، ۲=درمان، ۳=همه) نه `TypeVahed` (۱..۱۷، انواع واحد سازمانی). دادهٔ زندهٔ فاز ۱۲ فقط `{1,3}` داشت که با `TypeKoli` کاملاً سازگار است و با `TypeVahed` بی‌معنا بود. ⚠️ **این شاهد قوی است نه اثبات روی دادهٔ خودمان** — برچسب مقادیر روی دیتابیس عملیاتی راستی‌آزمایی نشده. ضمناً `VAHEDTYPE_ID` (FK به `TB_VAHED_TYPE`) موجود کاملاً متفاوتی است و نباید با این اشتباه شود.
- **🟡 چندمستأجری اگر پیاده شود، باید `Behavior` سراسری باشد.** پروژهٔ مرجع نشان داد اعمال per-handler (فقط ۱۲ از ۳۷۲ Handler) عملاً یعنی فراموش‌شدن. این درسی برای ریسک باز خودمان («چندمستأجری مرز ایزولاسیون نیست») است، نه یک تصمیم جدید.

### 🔴 تصمیمات باز جدید — کشف‌شده در فاز ۱۳ (۲۰۲۶-۰۸-۲۷)

طبق Accounting Safety Gate، هیچ‌کدام بی‌صدا رد نشد و هیچ‌کدام حدس زده نشد:

- **🔴 سه ستون `bool?`/`bool` مشکوک حالا در مسیر نوشتن *فعال* قرار گرفتند.** تا پیش از این فاز، فهرست ۱۹ ستونیِ «`bool?` که واقعاً enum است» (فاز ۱۲) فقط روی `TB_ACCOUNTCODE`/`TB_VOUCHERSHEAD` عملیاتی بود. حالا سه مورد دیگر از طریق API قابل نوشتن‌اند:
  - **`TB_WHITEANDBLACKLIST.STATE` — تأییدشده غلط.** طبق `docs/centralaccount-business-reference.md` بخش ۱۰-۲ ردیف ۱۲، واقعاً `StateEnum` سه‌مقداری است (۱=مجاز، ۲=فقط‌سیستمی، ۳=غیرمجاز). ما آن را `bool?` می‌پذیریم و می‌نویسیم. **یعنی مقدار «۳=غیرمجاز» از طریق API فعلی اصلاً قابل بیان نیست** و مقدار ۲ احتمالاً به‌غلط `true` خوانده می‌شود.
  - **`TB_ACCOUNTCODE_INTERFACE.TYPE` — ~~مشکوک~~ 🔴 تأیید شد که غلط است (۲۰۲۶-۰۹-۰۶، فاز ۱۷).** استدلال قبلی («اگر واقعاً دومقداری باشد `bool` بی‌خطر است») **باطل شد**: `InterfaceType` مقادیرش `{openVoucher=1, closeVoucher=2}` است، یعنی دامنه `{1,2}` نه `{0,1}`. `false` مقدار `0` می‌نویسد که اصلاً در دامنه نیست. رجوع `docs/centralaccount-business-reference.md` §۲۴-۱.
  - **`TB_PERSON_ACTION.OPERATORROLE` — ~~مشکوک~~ 🔴 قطعی شد (۲۰۲۶-۰۹-۰۶، فاز ۱۷).** در `Tamin.Core/Entities/PersonActions/PersonAction.cs` این پراپرتی صراحتاً از نوع `OperatorRole` (۱=مسئول امور مالی، ۲=رئیس واحد، ۳=جانشین امور مالی، ۴=جانشین رئیس واحد) اعلام شده — چهارمقداری. ⚠️ توجه: `TB_PERSON_ACTION.STATUS` که کنارش است در همان فایل `bool?` است و **واقعاً بولین است** — این دو نباید با هم اشتباه شوند.
  - در XML doc هر Command/DTO متأثر، هشدار صریح ثبت شد که ستون زیرین `NUMBER(1)` است و ممکن است چندمقداری باشد. **رفع نشد چون خارج از دامنهٔ صریح این فاز بود** (Taskی جداست) و چون نوع CLR عوض‌کردن یک breaking change در قرارداد API است.
- **🔴 IDOR حالا به ۸ Entity دیگر هم رسید.** همهٔ ریسک‌های authorization فازهای ۷/۸/۱۰ عیناً روی هر ۳۹ Endpoint جدید برقرارند: هر کاربر احراز‌شده می‌تواند هر رکوردی از هر واحد سازمانی را بخواند/بسازد/ویرایش/حذف کند. `VAHEDCODE` همچنان سمت سرور اعمال نمی‌شود — و توجه کنید که `TB_BILL_LOG` و `TB_PERSON_ACTION` هر دو ستون `VAHEDCODE` دارند که کاملاً ورودی فراخوان است. **سطح حمله بزرگ‌تر شد، ماهیت ریسک تغییر نکرد. مسدودکنندهٔ استقرار.**
- **🟡 هیچ تست repository واقعی (SQLite) برای این ۸ Entity نوشته نشد.** فازهای ۹/۱۰/۱۱ برای منطق cascade تست واقعی repository داشتند؛ این فاز ندارد چون منطق repositoryها تماماً CRUD ساده است. پیامد: **صحت Fluent Mapping و ترجمهٔ فیلتر `ISDELETED != true` برای این ۸ جدول اثبات‌نشده است** — به‌ویژه برای سه جدولی که `ISDELETED` آن‌ها `bool?` است و منطق سه‌مقداری SQL دارند.
- **🟡 `PersonAction` قاعدهٔ «یک نقش فعال به‌ازای هر شخص» را ندارد.** طبق `docs/centralaccount-business-reference.md`، پروژهٔ مرجع برای این Entity گاردهای `HasActiveRoleAsync`/`HasActiveUserAsync`/`HasActiveRoleExceptAsync` دارد که تضمین می‌کنند هر شخص فقط یک نقش فعال داشته باشد. **ما این را عمداً پیاده نکردیم** (طبق تصمیم «Legacy جایگزین کامل»، هیچ invariant بازسازی نمی‌شود مگر تصمیم صریح). UNIQUE موجود `UK_PERSON_ACTION(USERID, FROMDATE, TODATE)` این قاعده را **پوشش نمی‌دهد** — دو نقش فعال با بازهٔ تاریخ متفاوت کاملاً مجازند. اگر این قاعده لازم است، باید صریحاً در Application بازسازی شود.
- **🟡 `WhiteAndBlackList`/`WhiteList` در پروژهٔ مرجع هرگز در مسیر نوشتن اعمال نمی‌شوند.** سند مرجع صریحاً می‌گوید جست‌وجوی `whiteListRepository`/`whiteAndBlackListRepository` در کل `Commands` آن پروژه **صفر نتیجه** خارج از ماژول خودش داد — یعنی این ماتریس مجوز آنجا داده‌ای است که نوشته می‌شود ولی هیچ‌جا چک نمی‌شود. ما هم فقط CRUD ساختیم و هیچ enforcement ای اضافه نکردیم. **اگر انتظار می‌رود این لیست‌ها واقعاً دسترسی را محدود کنند، آن منطق هنوز هیچ‌جا وجود ندارد.**
- **🟡 `PreDescrib` هیچ مسیر حذفی ندارد و این دائمی است، نه موقت.** رکورد اشتباه ثبت‌شده از طریق API **قابل حذف نیست** (فقط قابل ویرایش). اگر حذف لازم باشد، تصمیم صریح می‌خواهد: افزودن ستون `ISDELETED` به schema (که این پروژه هرگز انجام نمی‌دهد)، یا مجازکردن حذف فیزیکی فقط برای این جدول (که استثنای معماری است).
- **🟡 `TB_BILL_LOG` در پروژهٔ مرجع فقط `Add` دارد، ما CRUD کامل دادیم.** آنجا یک **لاگ** است (لاگ شکست صورتحساب ماهانه) که فقط از `InvoiceConfirmation` نوشته می‌شود و بعد فقط گزارش گرفته می‌شود. ما Update/Delete هم ساختیم چون ستون‌هایش اجازه می‌دادند. **آیا قابل‌ویرایش‌بودن یک ردیف لاگ درست است؟** حدس زده نشد — اگر ماهیتش append-only است، باید Update/Delete حذف شود.

### 🔴 تصمیمات باز جدید — کشف‌شده در فاز ۱۴ (۲۰۲۶-۰۸-۲۸)

طبق Accounting Safety Gate هیچ‌کدام بی‌صدا رد نشد و هیچ‌کدام حدس زده نشد:

- **🔴 نوشتن روی `TB_VAHED_INFO` هیچ ردّ Audit ای ندارد.** این جدول **هیچ** ستون Audit ای ندارد (نه `ADDUSERID`، نه `CHANGEUSERID`، نه `CREATEDDATE`، نه `UPDATEDDATE`)، پس Handlerهایش عمداً `ICurrentUser` نمی‌گیرند. یعنی **ریشهٔ سلسله‌مراتب واحد سازمانی — که مرز چندمستأجری آیندهٔ پروژه به آن وابسته است — بدون هیچ ردّ حسابرسی قابل تغییر است.** ضمناً این تنها ✅ باقی‌مانده در جدول Accounting Safety Gate (یعنی Audit Trail) را برای این جدول نقض می‌کند. اگر Audit لازم است، تنها راه‌ها افزودن ستون به schema (که این پروژه هرگز انجام نمی‌دهد) یا یک جدول لاگ جانبی است — **تصمیم گرفته نشد.**
- **🔴 `CITY_ID` و `PARENT_ID` در `TB_VAHED_INFO` هیچ FK ای ندارند.** فقط `VAHEDTYPE_ID` FK دارد (`FK_VAHEDINFO_TYPE`). یعنی نگاشت مرکزی ORA-02291 → 400 برای آن دو ستون **کار نمی‌کند** و یک `CITY_ID`/`PARENT_ID` کاملاً نامعتبر **بی‌صدا نوشته می‌شود** — دقیقاً همان الگوی شکاف `TB_VOUCHERDETAIL_LINK_TAFSILI.TAFSILI_ID` از فاز ۱۱. pre-check عمداً اضافه نشد (اختراع قانون کسب‌وکاری).
- **🔴 حذف `LevelTafsil`/`TafsilGroup` هیچ بررسی وابستگی ندارد — و این دو از `AccountCode` هم حساس‌ترند.** هر دو Lookupهایی‌اند که **منبع حقیقت تفصیلی مجاز** (`TB_ACCOUNT_LINK_TAFSILGROUP`) مستقیماً به آن‌ها FK می‌زند، و `TB_LEVEL_TAFSIL` علاوه بر آن هدف ۷ جدول لینک دیگر است. چون حذف نرم است، هیچ FK اوراکلی شکایت نمی‌کند: می‌توان یک سطح/گروه تفصیلی را حذف کرد در حالی که لینک‌های فعالی به آن اشاره می‌کنند و **ناسازگاری کاملاً بی‌صدا** می‌ماند. هم‌خانوادهٔ ریسک 🔴 موجود «حذف گرهٔ کدینگ بدون بررسی وابستگی» (فاز ۸) است، ولی سطح انفجارش بزرگ‌تر است.
- **🟡 `UK_TBTAFSILGROUP` شامل خودِ `ISDELETED` است** (`TAFSILGROUP_CODE, ISDELETED`). یعنی soft-delete یک کد تفصیلی، آن کد را فقط **یک بار** آزاد می‌کند: ردیف حذف‌شدهٔ دومی با همان کد به ۴۰۹ می‌خورد. مشاهده ثبت شد، هیچ منطقی برایش اضافه نشد.
- **🟡 هفت ستون `NUMBER(1)` مشکوک دیگر وارد مسیر نوشتن فعال شدند.** قوی‌ترین مورد: **`TB_IDENTITYSUBGRPS.SUBGRPS_TYPE`** که کامنت خودِ اوراکل می‌گوید «نوع : حروف, اعداد, يا هردو» — یعنی صراحتاً **سه** مقدار، که `bool?` اصلاً نمی‌تواند نمایش دهد. مورد پرخطر بعدی **`TB_WORKSHOP.ISACTIVE`** است: خواهرِ ساختاریِ `TB_TAFSILI.ISACTIVE` که در فاز ۱۲ تأیید شد enum واقعی `{IsActive=1, DeActive=2}` است و `۰` اصلاً در آن نیست. بقیه: `ATTRIBBOXNO`/`FLAG`/`ATTRIBSUM`/`CONTROLID` روی `TB_ATTRIBFORACCOUNTCODE` و `PERSONTYPE` روی `TB_TAFSIL_GROUP`. **هیچ‌کدام تغییر نوع داده نشد** (دامنهٔ Task جدا)، ولی همگی در XML doc علامت‌گذاری شدند تا Task اصلاح پیدایشان کند.
- **🟡 `CHEQUE_IMAGE` و `CHECKFILE` (هر دو BLOB) بدون هیچ سقف اندازه‌ای پذیرفته می‌شوند.** اولین Endpointهای پروژه‌اند که داده باینری می‌گیرند (base64 در JSON). هیچ محدودیت اندازهٔ درخواست/آپلود در سطح API وجود ندارد و کنار نبودِ Rate Limiting یک مسیر سوءاستفادهٔ حافظه/فضاست. **سقف اختراع نشد** — سیاست آپلود یک تصمیم گرفته‌نشده است.
- **🟡 `TB_CHECK_TYPE` هیچ UNIQUE ندارد** پس ۴۰۹ اعلام نشد؛ یعنی **چند نوع چک با عنوان کاملاً یکسان قابل ثبت است**. همین برای `TB_LEVEL_TAFSIL` هم صادق است (کد/نام سطح تفصیلی می‌تواند تکراری باشد). اعلام ۴۰۹ گمانه‌زنی می‌بود.
- **🔴 IDOR حالا به ۸ Entity دیگر هم رسید** — هر ۳۹ Endpoint جدید زیر همان ریسک باز فازهای ۷/۸/۱۰/۱۳ اند. **نکتهٔ تشدیدکننده در این دسته:** `TB_VAHED_INFO` خودِ تعریف واحد سازمانی است، پس نبودِ authorization در سطح رکورد یعنی هر کاربر احراز‌شده می‌تواند **ساختار سازمانی‌ای را که قرار است مبنای چندمستأجری باشد** بازنویسی کند — آن هم بدون هیچ ردّ Audit ای (مورد اول بالا). **مسدودکنندهٔ استقرار.**

### 🔴 تصمیمات باز جدید — کشف‌شده در فاز ۱۵ (۲۰۲۶-۰۹-۰۵)

طبق Accounting Safety Gate هیچ‌کدام بی‌صدا رد نشد و هیچ‌کدام حدس زده نشد:

- **🔴 `TB_ELAMHEAD.ELAMHDRAMAD_TYPE` — قطعی‌ترین باگ `bool?`→enum کشف‌شده تا امروز، و از طریق API فعال است.** کامنت خودِ ستون در اوراکل می‌گوید « 3حق بيمه نوع اعلاميه 1ذي حسابي 2سايردرآمد» — یعنی صراحتاً **سه** مقدار (۱=ذی‌حسابی، ۲=سایر درآمد، ۳=حق بیمه). ما آن را `bool?` می‌پذیریم و می‌نویسیم، پس **مقدار «۳=حق بیمه» از طریق API فعلی اصلاً قابل بیان نیست** و مقدار ۲ احتمالاً به‌غلط `true` خوانده می‌شود. این از `TB_WHITEANDBLACKLIST.STATE` (فاز ۱۳) و `TB_IDENTITYSUBGRPS.SUBGRPS_TYPE` (فاز ۱۴) هم قوی‌تر است، چون هم کامنت اوراکل صریح است و هم شمارش مقادیر در خودِ کامنت آمده. **رفع نشد** چون تغییر نوع CLR یک breaking change در قرارداد API است و Taskی جداست.
- **🔴 `TB_ELAMHEAD.ELAMH_CASE` — همان الگو، با یک پیچ اضافه: مقادیرش ۱ و ۲ است، نه ۰ و ۱.** کامنت اوراکل: «نوع اعلاميه 1بد 2بس» (۱=بدهکار، ۲=بستانکار). حتی اگر واقعاً دومقداری باشد، نگاشت `bool?` روی مقادیر `{1,2}` **بی‌صدا غلط است** — چون `false` به `0` می‌نویسد که اصلاً در دامنهٔ مجاز نیست، و مقدار موجود `2` احتمالاً `true` خوانده می‌شود درست مثل `1`. یعنی برخلاف بقیهٔ موارد مشکوک، اینجا «دومقداری بودن» به‌تنهایی مشکل را حل نمی‌کند. هم‌الگوی `TB_TAFSILI.ISACTIVE` (فاز ۱۲) که `{IsActive=1, DeActive=2}` بود و `۰` در آن نبود.
- **🟡 سه ستون `NUMBER(1)` مشکوک دیگر وارد مسیر نوشتن فعال شدند.** `TB_RECEIP.RECEIPT_KIND` (غیر-nullable `bool`؛ نامش *kind* است نه یک پرچم، و در پروژهٔ مرجع مفهوم «فیش/حواله» دو نوع مدرک بانکی است)، `TB_BANKCARTDETAIL.CHECKRECEIPTTYPE` (کامنت اوراکل: «نوع مدرك بانكي (فيش يا حواله)» — باز هم یک *نوع*)، و `TB_CHECKBOOK.CHECKBOOK_TYPE`. هیچ‌کدام تغییر نوع داده **نشد**؛ همگی در XML doc علامت خوردند تا Task اصلاح پیدایشان کند. با این سه، فهرست انباشتهٔ ستون‌های مشکوک در مسیر نوشتن فعال به **~۱۷** رسید.
- **🔴 شش ستون شناسه در این دسته هیچ FK ای ندارند → مقدار نامعتبر بی‌صدا نوشته می‌شود.** نگاشت مرکزی ORA-02291 → 400 (فاز ۱۱) برای این‌ها **کار نمی‌کند**، چون اصلاً constraint ای وجود ندارد که نقض شود:
  - `TB_ACCOUNT.ACCOUNTTYPE_ID` — با اینکه جدول `TB_ACCOUNT_TYPE` واقعاً وجود دارد.
  - `TB_BANKCARTDETAIL.BANK_ID`, `BRANCH_ID`, `CHECK_INCORRENT_ID` — با اینکه `TB_BANK_LIST`/`TB_BANKBRANCH_LIST`/`TB_CHEQUES_INCORRENT` وجود دارند. (فقط `CHECK_ID` و `RECEIP_ID` این جدول FK دارند.)
  - `TB_CHEQUES_INCORRENT.CHECK_ID` — این جدول **هیچ** FK ای ندارد.
  - `TB_ELAMHEAD.WORKSHOP_ID` و `ELAMSENDERID` — فقط `VOUCHERSHEAD_ID` FK دارد.
  همان الگوی شکاف `TB_VOUCHERDETAIL_LINK_TAFSILI.TAFSILI_ID` (فاز ۱۱) و `TB_VAHED_INFO.CITY_ID`/`PARENT_ID` (فاز ۱۴). pre-check عمداً اضافه **نشد** (اختراع قانون کسب‌وکاری + مدیریت نادرست شرایط رقابتی).
- **🔴 حذف `ElamHead` هیچ cascade ای به `TB_ELAMDETAIL` ندارد.** `TB_ELAMDETAIL` عمداً خارج از scope ماند چون مرز Aggregate این جفت تصمیم‌گیری نشده. پیامد: soft-delete یک اعلامیه، ردیف‌های آن را **فعال** باقی می‌گذارد — دقیقاً همان وضعیتی که سند تا پیش از فاز ۹ داشت و آنجا ریسک 🔴 شناخته شد. **همین برای `CheckBook` → `TB_CHECK` هم صادق است** (حذف دسته‌چک، برگ‌های چکش را فعال می‌گذارد)، و برای سه جدول لینک تفصیلی تعبیه‌شدهٔ این دسته (`TB_ACCOUNT_LINK_TAFSILI`, `TB_EXPENCE_LINK_TAFSILI`, `TB_REVOLVINGFUND_LINK_TAFSILI`). **حدس زده نشد که کدام‌یک واقعاً باید cascade شوند** — نیازمند تصمیم صاحب پروژه، مثل تصمیم مرز Aggregate فاز ۱۰.
- **🟡 مرز Aggregate `ElamHead`/`ElamDetail` هنوز تصمیم‌گیری نشده.** فاز ۱۵ فقط Head را ساخت (الگوی `VoucherHead` فازهای ۵–۸). برای رسیدن به وضعیت معادل سند، این جفت به همان تصمیم «مدل ترکیبی» فاز ۱۰ نیاز دارد (CRUD مستقل دیتیل + composite create + cascade). **حدس زده نشد.**
- **🟡 `TB_ACCOUNT.CHECKFILE` سومین ستون BLOB بدون سقف اندازه است.** به `CHEQUE_IMAGE`/`CHECKFILE` فاز ۱۴ اضافه شد؛ همان ریسک باز «سیاست آپلود تصمیم‌گیری نشده» را بزرگ‌تر می‌کند. مثل `WorkShopDto`، عمداً از `BankAccountDto` **حذف** شد (لیست/جزئیات جای stream کردن داده باینری نیست) ولی در Create/Update قابل نوشتن است.
- **🟡 `TB_CHEQUES_INCORRENT` و `TB_RECEIP` هیچ UNIQUE ندارند** پس ۴۰۹ اعلام نشد؛ یعنی **دو رسید با شمارهٔ کاملاً یکسان، یا دو چک در جریان با همان `CHEQ_NO`، قابل ثبت‌اند**. اعلام ۴۰۹ بدون constraint متناظر گمانه‌زنی می‌بود (همان قضاوت فازهای ۱۰/۱۳/۱۴). ⚠️ برای `TB_RECEIP` این به‌ویژه قابل‌توجه است چون سه جدول (`TB_BANKCARTDETAIL`, `TB_PAYRECIVDETAIL`, `TB_VOUCHERSDETAIL`) به آن ارجاع می‌دهند.
- **🟡 `AK_AK_BANKCARTDETAIL_BANKCART` یک UNIQUE یازده‌ستونه است که همهٔ ستون‌هایش nullable اند.** در منطق سه‌مقداری اوراکل، ردیف‌هایی که در ستون‌های کلید `NULL` دارند معمولاً یکتایی را نقض نمی‌کنند — یعنی این constraint ممکن است در عمل بسیار ضعیف‌تر از آن چیزی باشد که به‌نظر می‌رسد. ۴۰۹ اعلام شد (constraint واقعاً وجود دارد)، ولی **رفتار واقعی‌اش روی دادهٔ زنده بررسی نشد**.
- **🔴 IDOR حالا به ۸ Entity دیگر هم رسید** — هر ۴۰ Endpoint جدید زیر همان ریسک باز فازهای ۷/۸/۱۰/۱۳/۱۴ اند. **نکتهٔ تشدیدکننده در این دسته:** `TB_ACCOUNT` شمارهٔ حساب بانکی، شبا و شمارهٔ کارت نگه می‌دارد و `TB_BANKCARTDETAIL` گردش بانکی — یعنی نبودِ authorization در سطح رکورد حالا **دادهٔ بانکی سازمان** را هم پوشش می‌دهد، نه فقط دادهٔ حسابداری. ضمناً ۷ تا از این ۸ جدول ستون `VAHEDCODE` دارند که کاملاً ورودی فراخوان است. **مسدودکنندهٔ استقرار.**

### 🔴 تصمیمات باز جدید — کشف‌شده در فاز ۱۶ (۲۰۲۶-۰۹-۰۶)

طبق Accounting Safety Gate هیچ‌کدام بی‌صدا رد نشد و هیچ‌کدام حدس زده نشد:

- **🔴 `TB_PAYRECIVHEAD.PAYRECIVTYPE` — باگ `bool?`→enum *تأییدشده* (نه مشکوک)، و از طریق API فعال است.** طبق `docs/centralaccount-business-reference.md` بخش ۱۰-۲ ردیف ۱۴، پروژهٔ مرجع همین ستون را با enum سه‌مقداری `PayRecivType` مدل می‌کند — **۱=پرداخت، ۲=دریافت، ۳=همه** — و خودِ جدول آن سند نگاشت `bool?` ما را صراحتاً 🔴 **غلط** علامت زده است. پیامدها: مقدار «۳=همه» از طریق API فعلی **اصلاً قابل بیان نیست**، و یک ردیف موجود با مقدار ۲ احتمالاً دقیقاً مثل ۱ به‌غلط `true` خوانده می‌شود. این از نظر قدرت شاهد هم‌ردهٔ `TB_ELAMHEAD.ELAMHDRAMAD_TYPE` (فاز ۱۵) است. **رفع نشد** چون تغییر نوع CLR یک breaking change در قرارداد API است و Taskی جداست؛ ولی برخلاف موارد قبلی، این‌بار یک **تست پین‌کننده** (`Handle_PayReciveType_RoundTripsOnlyTwoOfItsThreeRealValues`) نوع فعلی را قفل می‌کند تا اصلاح آینده ناچار باشد صریح و مرئی باشد. با این مورد، فهرست انباشتهٔ ستون‌های `bool?` مشکوک/غلط در مسیر نوشتن فعال به **~۱۸** رسید.
- **🔴 مرز Aggregate `TmpVoucherHead`/`TmpVoucherDetail` — سیگنال پروژهٔ مرجع «تعبیه‌شده» است، پس Head-only اینجا یک شکاف کارکردی واقعی است نه صرفاً scope.** در `D:\CentralAccount` هیچ پوشهٔ Command مستقلی برای `TmpVoucherDetail` وجود ندارد؛ `AddTmpVoucherHeadCommandHandler.cs:32-58` کل گراف `head + details` را می‌سازد و با **یک** `AddAsync` ذخیره می‌کند (`docs/centralaccount-business-reference.md` §5). **پیامد مشخص: API فعلی ما فقط می‌تواند سند موقتِ خالی بسازد** — نه در یک فراخوان و نه در چند فراخوان، هیچ راهی برای افزودن ردیف وجود ندارد. در نتیجه مسیر «ارتقای سند موقت به سند اصلی» (`AddVochersImportTempCommand` در پروژهٔ مرجع) از API ما در دسترس نیست و کل مفهوم «سند موقت» عملاً بی‌مصرف می‌ماند. این نیازمند همان تصمیم «مدل ترکیبی» فاز ۱۰ است. **حدس زده نشد.**
- **🟡 مرز Aggregate `PayReciveHead`/`PayReciveDetail` هم تصمیم‌گیری نشده — ولی سیگنالش عکسِ بالاست.** همان پروژهٔ مرجع `PayReciveDetail` را **Aggregate Root مستقل** مدل می‌کند (`PayAndreciveDetails\{CreatedetailSingle,UpdateDetailSingle,DeleteDetailSingle}`) **و هم‌زمان** سرسند را به‌صورت composite با ردیف‌هایش می‌سازد (`AddPayReciveCommand`) — یعنی دقیقاً همان شکل «ترکیبی» که صاحب پروژه در فاز ۱۰ برای سند انتخاب کرد. پس برای این جفت، شکل هدف تقریباً روشن است و فقط به تأیید نیاز دارد؛ برخلاف جفت `TmpVoucher*` که سیگنالش کاملاً تعبیه‌شده است. **هیچ‌کدام فرض نشد.**
- **🔴 حذف `PayReciveHead`/`TmpVoucherHead` هیچ cascade ای به فرزندانشان ندارد.** soft-delete یک سرسند دریافت/پرداخت، ردیف‌های `TB_PAYRECIVDETAIL` (و لینک‌های `TB_PAYRECIVDETAIL_LINK_TAFSILI` زیرشان) را **فعال** باقی می‌گذارد؛ همین برای `TmpVoucherHead`→`TB_TMP_VOUCHERSDETAIL` صادق است. دقیقاً همان وضعیتی که سند تا پیش از فاز ۹ داشت و آنجا ریسک 🔴 شناخته شد، و هم‌خانوادهٔ ریسک باز `ElamHead`/`CheckBook` از فاز ۱۵. **حدس زده نشد کدام‌یک واقعاً باید cascade شوند** — به تصمیم صاحب پروژه نیاز دارد و منطقاً باید هم‌زمان با تصمیم مرز Aggregate بالا گرفته شود.
- **🟡 `TB_TMP_VOUCHERHEAD.SOURCEID` هیچ FK ای ندارد** → مقدار نامعتبر بی‌صدا نوشته می‌شود؛ نگاشت مرکزی ORA-02291 → 400 (فاز ۱۱) برایش **کار نمی‌کند** چون constraint ای وجود ندارد که نقض شود. تنها FK این جدول `FK_TMP_VOCHERHEAD` روی `VOUCHERSHEAD_ID` است. همان الگوی `TB_VOUCHERDETAIL_LINK_TAFSILI.TAFSILI_ID` (فاز ۱۱)، `TB_VAHED_INFO.CITY_ID`/`PARENT_ID` (فاز ۱۴) و شش ستون فاز ۱۵. pre-check عمداً اضافه **نشد**.
- **🟡 هیچ‌کدام از دو جدول این دسته UNIQUE ندارند، پس ۴۰۹ اعلام نشد** — یعنی **دو سرسند دریافت/پرداخت با `PAYRECIVCODE` کاملاً یکسان قابل ثبت‌اند**، و همین‌طور دو سند موقت با `SOURCEID` یکسان (یعنی یک سند مبدأ می‌تواند دوبار stage شود بدون هیچ اعتراضی). ⚠️ نکتهٔ مهم: **پروژهٔ مرجع برای `PayReciveCode` یک گارد شمارهٔ تکراری در سطح Application دارد** (`AddPayReciveCommand`/`UpdatePayReciveCommand`، §21-6) — ما آن را عمداً بازسازی **نکردیم**، چون هیچ constraint ای در schema پشتش نیست و بازسازی‌اش هم اختراع قانون کسب‌وکاری است هم race-prone. اگر یکتایی شمارهٔ سند واقعاً لازم است، تصمیم صریح می‌خواهد.
- **🟡 گارد حذف پروژهٔ مرجع برای `PayReciveHead` بازسازی نشد.** آنجا `DeletePayReciveCommand` با پیام «برای ردیف انتخابی سند صادر شده و قابل حذف نمی باشد» مانع حذف سند دریافت/پرداختی می‌شود که قبلاً به سند حسابداری تبدیل شده. `DeletePayReciveHeadCommand` ما چنین بررسی‌ای ندارد و سندی را که `VOUCHERSHEAD_ID` پرشده دارد بدون هیچ اعتراضی soft-delete می‌کند.
- **🟡 تنها نقطهٔ enforce تراز در پروژهٔ مرجع دقیقاً روی همین Entity است — و آن مسیر اینجا وجود ندارد.** طبق بخش ۱۴ سند مرجع، `AddVoucherByPayAndReciveCommandHandler` تنها جایی در کل آن پروژه است که `Sum(Debtor) != Sum(Creditor)` را چک می‌کند (هنگام تبدیل سند دریافت/پرداخت به سند حسابداری). ما نه آن Command را داریم نه ردیف‌ها را، و `PayReciveHeadDto` هم جمع بدهکار/بستانکار برنمی‌گرداند — پس هیچ نقطهٔ طبیعی برای این تضمین در دستهٔ فعلی وجود ندارد. مرتبط با ریسک 🔴 موجود «تضمین تراز وجود ندارد» (فاز ۱۰).
- **🟡 `TB_TMP_VOUCHERHEAD` بی‌محافظ‌ترین جدول مسیر نوشتن تا امروز است: هر ستونش nullable است.** نه فقط `ISDELETED` (که الگوی آشنای `bool?` است)، بلکه **هر ستون کسب‌وکاری**. پیامد در ترکیب با PUT کامل: یک بدنهٔ تماماً `null` روی `POST {id}/update` هر فیلد کسب‌وکاری سند موقت را پاک می‌کند و فقط ردّ Audit باقی می‌ماند (با تست `Handle_AllNullBody_NullsOutEveryWritableField` قفل شد). این پیامد پذیرفته‌شدهٔ تصمیم PUT-به‌جای-PATCH فاز ۸ است، ولی روی این جدول شدیدتر از هر جای دیگر پروژه بروز می‌کند.
- **🟡 `TB_TMP_VOUCHERHEAD.SYS_TYPE` یک ستون یک‌کاراکتری با مجموعه‌مقادیر ناشناخته است.** چون `string` است نه عددی، الگوی `bool?`→enum رویش صدق نمی‌کند؛ ولی هیچ قاعدهٔ مقدار مجاز هم برایش اختراع نشد (فقط `MaximumLength(1)`). اگر مجموعهٔ مجاز مشخص شود، جای اعمالش Validator است.
- **🟡 باز هم هیچ تست repository واقعی (SQLite) نوشته نشد** — چهارمین دستهٔ پیاپی (۱۳/۱۴/۱۵/۱۶) با همین شکاف. پیامد مشخص برای این دسته: صحت Fluent Mapping و به‌ویژه **تفاوت ترجمهٔ فیلتر بین `== false` (روی `TB_PAYRECIVHEAD` غیر-nullable) و `!= true` (روی `TB_TMP_VOUCHERHEAD` سه‌مقداری)** اثبات‌نشده مانده — دقیقاً همان‌جایی که فاز ۹ نشان داد منطق سه‌مقداری SQL می‌تواند بی‌صدا اشتباه شود.
- **🔴 IDOR حالا به ۲ Entity دیگر هم رسید** — هر ۱۰ Endpoint جدید زیر همان ریسک باز فازهای ۷/۸/۱۰/۱۳/۱۴/۱۵ اند. هر دو جدول ستون `VAHEDCODE` دارند که کاملاً ورودی فراخوان است و سمت سرور اعمال نمی‌شود. **نکتهٔ تشدیدکننده:** `TB_TMP_VOUCHERHEAD` جدول staging ورودی سند است (در پروژهٔ مرجع مقصد پیام‌های Kafka)، یعنی نبودِ authorization در سطح رکورد روی آن، دستکاری صف ورودی دفتر حسابداری را ممکن می‌کند. **مسدودکنندهٔ استقرار.**

### 🔴 تصمیمات باز جدید — کشف‌شده در فاز ۱۹ (اعمال سراسری `VahedCode`، ۲۰۲۶-۰۹-۰۹)

اولین فاز کاملاً امنیتی پروژه. جزئیات کامل در `docs/phase-log.md` بخش «فاز ۱۹».

#### وضعیت ریسک 🔴 #۱ (IDOR) — نصفه بسته شد

- **✅ بسته شد — سمت لیست/جستجو/ساخت.** `VahedCode` دیگر هرگز از ورودی client گرفته نمی‌شود؛ `VahedScopeBehavior` آن را بی‌قیدوشرط از `ICurrentUser.VahedCode` تحمیل می‌کند و هر ۱۹ `ReadRepository` فیلتر `== vahedCode` بی‌قیدوشرط دارند. شامل ۳۸ Command (Create+Update) و ۲۲ Query (۱۹ لیست + ۳ گزارش تراز).
- **🔴 همچنان باز — دسترسی مستقیم با `id`.** رجوع به مورد بعدی.

#### 🔴 IDOR روی `GetById`/`Update`/`Delete` عمداً باز ماند — **تصمیم آگاهانه، نه سهل‌انگاری**

**این را به‌درخواست صریح صاحب پروژه اینجا ثبت می‌کنیم تا هیچ‌وقت با «فراموش شد» اشتباه گرفته نشود.**

هر کاربر احراز‌شده‌ای که `id` یک رکورد را بداند، همچنان می‌تواند رکورد **هر واحد سازمانی دیگری** را:
- بخواند (`GET /api/x/{id}`)،
- ویرایش کند (`POST /api/x/{id}/update`)،
- حذف نرم کند (`POST /api/x/{id}/delete`).

هیچ بررسی مالکیت رکوردی روی این سه مسیر اضافه **نشد**. کاربر با علم کامل به این پیامد، دامنهٔ فاز را به لیست/جستجو/ساخت محدود کرد.

**آنچه در `Update` بسته شد** محدودتر از آن چیزی است که ابتدا نوشته شده بود (اصلاح‌شده در ۲۰۲۶-۰۹-۰۹ پس از یافتهٔ دو Agent مستقل در پاس `/code-review` فاز ۱۹): فیلد `VahedCode` دیگر ورودی caller نیست، بلکه همیشه `VahedScopeBehavior` آن را با واحد سازمانی **خودِ کاربر احراز‌شده** بازنویسی می‌کند. این فقط جلوی انتقال رکورد به یک واحد **دلخواه/شخص‌ثالث** را می‌گیرد؛ چون `GetForUpdateAsync` رکورد را فقط با `Id` می‌خواند (بدون فیلتر `VAHEDCODE`) و Handler سپس `entity.VAHEDCODE = request.VahedCode` را بی‌قیدوشرط اعمال می‌کند، کاربری که `Id` یک رکورد از واحد دیگر را بداند همچنان می‌تواند با یک `POST {id}/update` آن رکورد را **به واحد خودش منتقل کند** (سرقت رکورد به نفع خودش) — این پیامد از قبل از فاز ۱۹ هم وجود داشت (وقتی `VahedCode` کاملاً آزاد بود، مقصد می‌توانست هر واحدی باشد؛ حالا مقصد فقط به واحد خودِ کاربر محدود شده، نه حذف‌شده).

سطح حملهٔ باقی‌مانده شامل هم *خواندن*/*حذف* بدون فیلتر و هم این *سرقت با Update* است، روی همان دادهٔ حساسی که در ریسک #۱ فهرست شده بود: شماره حساب/شبا/کارت بانکی، گردش بانکی، صف staging ورودی سند، و کل سند حسابداری.

**برای بستن کامل لازم است:** یا بررسی مالکیت در سطح Handler/Repository (خواندن رکورد و مقایسهٔ `VAHEDCODE` با `ICurrentUser.VahedCode` پیش از هر عملیات)، یا یک Behavior دوم که همین کار را سراسری انجام دهد. **هیچ‌کدام حدس زده نشد.**

#### 🔴 باگ جدید — `ValidationBehavior` هرگز برای هیچ Update/Delete اجرا نشده است

**کشف‌شده حین فاز ۱۹، مستقلاً تأیید شد، عمداً در این فاز رفع نشد.**

`ValidationBehavior<TRequest,TResponse>` قید `where TRequest : IRequest<TResponse>` دارد. در MediatR 12 به بعد (این پروژه: **۱۴.۲.۰**) `IRequest` غیرجنریک دیگر از `IRequest<Unit>` ارث نمی‌برد. همهٔ Update/Delete commandهای پروژه `: IRequest` (void) اند. کانتینر DI هنگام resolve کردن open-generic، هر registration ای را که قیدش برآورده نشود **بی‌صدا** رد می‌کند — بدون exception، بدون log.

**پیامد: از فاز ۸ تا امروز، FluentValidation روی هیچ ورودی `Update` یا `Delete` اجرا نشده.** یعنی همهٔ `RuleFor`های آن Validatorها (طول رشته، بازهٔ عددی، `NotEmpty`) در عمل بی‌اثر بوده‌اند و ورودی نامعتبر مستقیم به Handler و از آنجا به Oracle می‌رفته.

تست‌ها این را نگرفتند چون تست‌های Validator مستقیماً کلاس Validator را instantiate می‌کنند و از pipeline عبور نمی‌کنند — یعنی **پوشش تست بالا اینجا اصلاً سیگنال نبوده**.

⚠️ `VahedScopeBehavior` عمداً این قید را **ندارد** (`where TRequest : notnull`) تا قربانی همین باگ نشود. **هر کس این را «تمیزکاری» کند، بی‌صدا IDOR را روی همهٔ Update commandها بازمی‌گرداند.** در XML doc خود فایل هم هشدار داده شده.

**چرا در فاز ۱۹ رفع نشد:** فعال‌کردن یک‌بارهٔ validation روی ~۲۹ Update و ~۲۸ Delete می‌تواند شکست‌های واقعی زیادی رو کند (هم در تست‌های API، هم در رفتار واقعی) و فاز مستقل با `qa-tester` می‌خواهد. **تصمیم رفع/زمان‌بندی با صاحب پروژه.**

#### 🔴 `PersonAction` — قاعده‌ای که schema پشتیبانی نمی‌کند (منتظر تصمیم کاربر)

کاربر قاعده‌ای داد: «`VahedCode` هرگز خالی نباشد، **به‌جز `PersonAction`** که به‌جایش می‌شود با `VahedType` دسترسی ایجاد کرد.»

**verify شد (حدس زده نشد): `TB_PERSON_ACTION` هیچ ستون `VAHEDTYPE_ID` ندارد.** ستون‌هایش: `ID`, `USERNAME`, `USERID`, `FROMDATE`, `TODATE`, `STATUS`, `OPERATORROLE`, `VAHEDCODE`, `UPDATEDDATE`, `ADDUSERID`, `CHANGEUSERID`, `ISDELETED`, `CREATEDDATE`. تأیید از دو منبع: خود Entity و Fluent Mapping (`LegacyDbContext.cs` خطوط ۲۳۳۱–۲۳۶۵). تنها index: `UK_PERSON_ACTION` روی `(USERID, FROMDATE, TODATE)`.

**سرنخ قوی برای پاسخ:** فقط ۵ جدول در کل schema ستون `VAHEDTYPE_ID` واقعی دارند — `TB_WHITELIST`, `TB_WHITEANDBLACKLIST`, `TB_ACCOUNTEXCEPTION`, `TB_RABET_CLOSING`, `TB_VAHED_INFO`. چهار تای اول **هیچ‌کدام `VAHEDCODE` ندارند**. به‌ویژه `TB_WHITELIST`/`TB_WHITEANDBLACKLIST` دقیقاً همان الگویی‌اند که کاربر توصیف کرد (دسترسی بر اساس **نوع** واحد به‌جای یک واحد مشخص).

**وضعیت فعلی:** `PersonAction` کاملاً دست‌نخورده ماند و **تنها Entity دارای `VAHEDCODE` است که هنوز `VahedCode` را از caller می‌گیرد** — یعنی ریسک #۱ روی این یک Entity به‌طور کامل (شامل لیست و Create) باز است.

**سؤال باز برای صاحب پروژه:** آیا منظور `WhiteList`/`WhiteAndBlackList` بوده، یا انتظار ستونی روی `TB_PERSON_ACTION` می‌رفته که وجود ندارد؟ اگر دومی، پیاده‌سازی مستلزم **افزودن ستون به Oracle** است که طبق «Legacy جایگزین کامل» این پروژه هرگز انجام نمی‌دهد.

#### 🔴 یافتهٔ Gate امنیتی — `PersonAction` یک بمب ساعتی ارتقای سطح دسترسی است

`security-reviewer` (Verdict: **Pass-with-findings**) ارزیابی کرد که شکاف `PersonAction` جدی‌تر از «منتظر تصمیم `VahedType` بمانیم» است.

علاوه بر Create/Update، **`GetPersonActionsQuery` هم unscoped است** — یعنی هر کاربر احراز‌شده‌ای می‌تواند ردیف‌های `TB_PERSON_ACTION` **همهٔ واحدها** را فهرست کند، شامل `USERID` (که **کد ملی** است، رجوع فاز ۱۷) و `OPERATORROLE`.

**ارزیابی:** تا وقتی هیچ منطق authorization ای در سیستم از `TB_PERSON_ACTION` برای تصمیم دسترسی نمی‌خواند (امروز درست است، چون RBAC هنوز وجود ندارد)، این شکاف قابل تحمل است. **ولی خنثی نیست:** لحظه‌ای که RBAC روی این جدول ساخته شود — که هدف بدیهی این جدول است («مسئول امور مالی/رئیس واحد») — مهاجمی که **امروز** یک ردیف `PersonAction` با `VahedCode` دلخواه و `UserId` خودش ساخته باشد، از همان لحظه یک مجوز جعلی در دست دارد.

**توصیهٔ صریح `security-reviewer`: این باید اولین کاری باشد که هنگام شروع کار RBAC انجام می‌شود، نه چیزی که ضمنی حل شود.**

#### 🔴 یافتهٔ Gate امنیتی — نوشتن بین‌واحدی از طریق `CreateVoucherDetail`

`CreateVoucherDetailCommandHandler` برای پیداکردن سرسند والد از `GetForUpdateAsync` استفاده می‌کند که **خودش scope نمی‌خورد**. پیامد: فراخوان می‌تواند یک ردیف سند جدید — که به‌درستی با `VahedCode` **خودش** مهر خورده — به سرسندی متعلق به **واحد دیگری** بچسباند، صرفاً با دانستن `Id` آن سرسند.

این پیامد مستقیم تصمیم آگاهانهٔ «بررسی مالکیت رکورد خارج از scope است» است و در XML doc خود `CreateVoucherDetailCommand.cs` (خطوط ۲۴–۲۹) هم به‌عنوان عدم‌تقارن حل‌نشده مستند شده. **ولی چون یک *نوشتن* است نه صرفاً یک *خواندن*، جداگانه ثبت می‌شود** تا در فهرست معافیت‌های GetById گم نشود.

**تصمیم لازم:** یا `CreateVoucherDetailCommandHandler` پیش از اجازهٔ چسباندن ردیف، تطابق `VAHEDCODE` سرسند با `ICurrentUser.VahedCode` را بررسی کند، یا صریحاً کنار بقیهٔ معافیت‌های GetById پذیرفته شود. **حدس زده نشد.**

#### 🟡 موارد دیگر فاز ۱۹

- **`TB_ACCOUNTCODE` (درخت کدینگ حساب) ستون `VAHEDCODE` ندارد** → چارت حساب ذاتاً سراسری است و این فاز اصلاً محدودش نکرد. با انتظار شهودی «همه‌چیز واحدی می‌شود» فرق دارد. اگر ایزولاسیون چارت حساب لازم است، مسئلهٔ جداگانه‌ای است.
- **`TB_VAHED_INFO` عمداً سراسری ماند** (تصمیم صریح صاحب پروژه) — اگر Create مقدار را تحمیل کند هرگز نمی‌توان واحد جدید ساخت؛ اگر لیست فیلتر شود هیچ UI ای dropdown واحد نمی‌تواند بسازد. با ریسک باز «نوشتن روی `TB_VAHED_INFO` هیچ Audit ای ندارد» ترکیب می‌شود: این جدول هم بدون ردّ حسابرسی است، هم بدون محدودیت واحد.
- **رکوردهای `VAHEDCODE IS NULL` حالا برای همه نامرئی‌اند** (fail-closed، تصمیم صریح). ۱۲ از ۲۱ Entity دارای `VAHEDCODE` ستون nullable دارند؛ اگر دادهٔ Legacy موجود ردیف با `NULL` داشته باشد، آن ردیف‌ها از همهٔ لیست‌ها ناپدید شده‌اند. **روی دادهٔ واقعی بررسی نشد** که چند ردیف چنین‌اند.
- **claim `vahed_code` همچنان با توکن واقعی IDP تأیید نشده** (ریسک باز #۱۰ دست‌نخورده) — ولی حالا پیامدش بزرگ‌تر است: اگر نام claim غلط باشد، `MissingVahedScopeException` می‌دهد و **هر Endpoint لیست/ساخت با 403 می‌خوابد**. طراحی عمداً fail-loud است، ولی این یعنی اولین تست با توکن واقعی باید پیش از استقرار انجام شود.
- **نقش‌بندی (Role-based) اعمال نشد** — ماتریس ۷ نقش سازمان استخراج‌شده در فاز ۱۷ همچنان بلااستفاده است.

### 🔴 تصمیمات باز جدید — کشف‌شده در فاز ۱۸ (تراز آزمایشی ۴/۶/۸، ۲۰۲۶-۰۹-۰۷)

اولین فاز گزارشی پروژه. طبق Accounting Safety Gate هیچ‌کدام از این موارد بی‌صدا رد نشد و **هیچ‌کدام حدس زده نشد**. جزئیات کامل در `docs/phase-log.md` بخش «فاز ۱۸».

- **✅ ~~معنای `FirstDebtor`/`FirstCreditor` (مانده اول دوره)~~ — حل شد (۲۰۲۶-۰۹-۰۷، تصمیم صریح صاحب پروژه).**
  سؤال: فرمول صریح پروژهٔ مرجع `FirstDebtor = TotDebtor - CurDebtor` (گردش خام انباشتهٔ پیش از دوره، بدون یک‌طرفه‌سازی) یا عرف تراز آزمایشی (ماندهٔ یک‌طرفهٔ خالص، مثل `DebtorBalance`)؟
  **تصمیم: «همیشه باید یک طرف مانده غیر صفر داشته باشد»** — یعنی یک‌طرفه‌سازی (`Math.Max`) بر وفاداری به فرمول خام مرجع اولویت دارد.
  - `GetTrialBalance6QueryHandler`/`GetTrialBalance8QueryHandler` اصلاح شدند: `FirstDebtor = Math.Max(OpeningDebtor - OpeningCreditor, 0)` و متقارنش برای `FirstCreditor` — دقیقاً هم‌الگو با `DebtorBalance`/`CreditorBalance`.
  - **پیامد جانبی:** در فرمت ۸ ستونه، ستون‌های `TotDebtor`/`TotCreditor` (گردش تجمیعی خام) عمداً **خام باقی ماندند** (نه یک‌طرفه) چون گردش، برخلاف مانده، می‌تواند هر دو طرفش هم‌زمان غیرصفر باشد — پس invariant قبلی `TotDebtor == FirstDebtor + Debtor` دیگر **به‌طور کلی برقرار نیست** (فقط وقتی `FirstDebtor`/`FirstCreditor` خام بودند برقرار بود). تستی که این invariant رو assert می‌کرد حذف و با تست netting جایگزین شد.
  - ۲۱۰۸/۲۱۰۸ تست سبز پس از این اصلاح (از ۲۱۰۹ — یک theory ۳موردی با یک fact جایگزین شد).
- **✅ ~~آیا View های `VWTRIALREPORTWRAPPER`/`VWTRIALREPORTWRAPPERTAFSILI` روی Oracle *ما* وجود دارند؟~~ — حل شد با کوئری Read-Only روی `ALL_VIEWS` (۲۰۲۶-۰۹-۰۷).**
  کوئری واقعی روی Oracle زندهٔ `setadidevdb.tamin.org` اجرا شد: schema `CENTRALACCOUNT` دقیقاً **۲۸ View** دارد (منطبق با شمارش فاز Reverse Engineering) و فهرست کامل نامشان حالا ثبت شده است.
  - **۱۹ تا از ۲۰ View پروژهٔ مرجع وجود دارند**، از جمله هر دو موردِ مسدودکنندهٔ Drill-down: `VWTRIALREPORTWRAPPER` ✅ و `VWTRIALREPORTWRAPPERTAFSILI` ✅. پس **batch بعدی (Drill-down) مسدود نیست.**
  - **تنها View غایب: `VWTRIALBALANCEREPORT4`** — نه در `CENTRALACCOUNT`، نه به‌عنوان Synonym، نه تحت هیچ owner دیگری (با `ALL_OBJECTS` روی الگوی `%TRIALBALANCE%`/`%TRIALREPORT%` چک شد؛ فقط همان دو Wrapper برگشتند). ⚠️ طنز ماجرا: این تنها View غایب، دقیقاً همانی است که اسمش به گزارش تراز ۴ ستونهٔ فاز ۱۸ می‌خورَد — که خوشبختانه ما آن را با SQL دست‌نویس روی جدول‌های پایه ساختیم، نه از روی View. اگر بر آن View تکیه کرده بودیم، فاز ۱۸ روی Oracle واقعی از کار می‌افتاد.
  - **۹ View که در سند مرجع نبودند و ما نمی‌شناختیم** (احتمالاً برای گزارش‌های آینده مفید): `VWCLOSINGBALANCE`, `VWCLOSINGBALANCE2`, `VWFIRSTBALANCE`, `VW_AGEPYRAMIDREPORT`, `VW_BEGINVOUCHER`, `VW_BEGINVOUCHER2`, `VW_CLOSINGVOUCHERDETAILS`, `VW_CONSOLIDATE_REPORT2`, `VW_CONSOLIDATE_REPORT_OLD`. `VWFIRSTBALANCE` از نظر نام مستقیماً به بحث `FirstDebtor`/`FirstCreditor` بالاتر مربوط است و ارزش بررسی دارد.
  - همچنان **صفر View در `LegacyDbContext` اسکفولد شده** و هیچ `ToView` وجود ندارد — این بخش تغییری نکرد.
- **✅ ~~متن SQL هرگز روی Oracle واقعی اجرا نشده~~ — حل شد (۲۰۲۶-۰۹-۰۷). SQL روی Oracle زنده اجرا شد و درست کار کرد.**
  دقیقاً همان متن SQL که `TrialBalanceReadRepository.GetAggregatesAsync` می‌سازد (سطح `Moin`، `year='1404'`، بقیهٔ پارامترها `NULL`) با bind واقعی روی دیتابیس زنده اجرا شد:
  - **بدون هیچ خطای نحوی/معنایی.** سه self-join، `NVL`/`CASE`، `GROUP BY`/`ORDER BY` روی عبارت درون‌ریزی‌شده، و **استفادهٔ مکرر از `:toDate`/`:fromDate` در چند نقطه** همگی درست اجرا شدند.
  - **۱۴ ردیف** برگرداند با کدهای معین ۶ رقمی معنادار (`201010`, `303002`, `601010`, …) و `Description` غیرخالی. جمع کل: `TotalDebtor = 21,823,731,508` و `TotalCreditor = 1,803,305,610`.
  - **تأیید تطابق نوع پارامتر:** `TB_VOUCHERSHEAD.YEAR` واقعاً `CHAR(4)` است، پس bind شدنِ `year` با `OracleDbType.Char` درست است (نه `Varchar2`). `DATE_DOC` هم `VARCHAR2(8)` تأیید شد، یعنی فرض «مقایسهٔ رشته‌ای روی جلالی zero-padded» درست است.
  - ⚠️ **آنچه این اجرا اثبات نکرد:** فقط سطح `Moin` و فقط با پارامترهای `NULL` اجرا شد. شاخه‌های `Group`/`Kol` و مسیرهای `fromDate`/`toDate`/`vahedCode`/`docLife` روی Oracle واقعی هنوز اجرا نشده‌اند (منطقاً هم‌شکل‌اند، ولی این **استنتاج** است نه مشاهده). ضمناً این اجرا با `sqlplus` بود، نه از طریق EF Core `SqlQueryRaw` — یعنی materialization به `TrialBalanceAggregateRow` و رفتار `BindByName` درایور همچنان اثبات‌نشده‌اند.
  - ⚠️ **فیلترهای `ISDELETED` عملاً روی این داده تمرین نشدند:** هر ۱۹ سرسند soft-delete شدهٔ سال ۱۴۰۴ **هیچ ردیف دیتیلی ندارند** و صفر ردیف `TB_VOUCHERSDETAIL` هم `ISDELETED=1` دارد. پس نتیجهٔ گزارش با و بدون آن دو فیلتر یکسان است و صحتشان روی دادهٔ زنده تأیید **نشد**.
- **✅ ~~JOIN غیرشرطی به `ak`/`ag` می‌تواند ردیف‌های معتبر را بی‌صدا حذف کند~~ — یافتهٔ `/code-review` (۲۰۲۶-۰۹-۰۷)، همان‌جا رفع شد.** SQL اصلی هر سه سطح (گروه/کل/معین) را با `JOIN` معمولی به `ak`/`ag` (والدهای کل/گروه) می‌ساخت. چون این پروژه سلسله‌مراتب ثابت سه‌سطحی را آگاهانه کنار گذاشته (رجوع «تصمیم معماری دوم»)، زنجیرهٔ `PARENTID` یک حساب معین لزوماً کامل نیست — اگه شکسته باشه، ردیف از **هر سه سطح گزارش** بی‌صدا حذف می‌شد، حتی از گزارش سطح معین که اصلاً به `ak`/`ag` نیازی نداره. با `LEFT JOIN` رفع شد: سطح معین دیگه تحت‌تأثیر زنجیرهٔ شکسته نیست؛ سطح گروه/کل برای چنین ردیفی یک گروه `NULL` قابل‌مشاهده («زنجیرهٔ نامعلوم») نشون می‌ده به‌جای حذف بی‌صدا.
  - **✅ روی Oracle زنده verify شد (۲۰۲۶-۰۹-۰۷) — نتیجه: این اصلاح صرفاً *احتیاطی* بود، نه رفعِ یک باگ زندهٔ فعلی.** چهار کوئری مستقل زده شد: هر **۶۰** حساب `TYPECODE=3` والد کل‌شان (`ak`) را resolve می‌کند (صفر `PARENTID` نال، صفر `PARENTID` معلق) و هر **۶۰** تا والد گروه‌شان (`ag`) را هم resolve می‌کند. تعداد ردیف‌هایی که با `JOIN` معمولی از گزارش حذف می‌شدند: **صفر** برای سال ۱۴۰۴ و **صفر در کل جدول برای همهٔ سال‌ها**. یعنی روی دادهٔ فعلی، خروجی `JOIN` و `LEFT JOIN` byte-identical است.
  - **با این حال `LEFT JOIN` باید بماند و این تصمیم معکوس نشود.** دلیل ساختاری است نه آماری: این پروژه سلسله‌مراتب ثابت سه‌سطحی را **آگاهانه کنار گذاشته** (تصمیم معماری دوم) و هیچ constraint ای در schema کامل‌بودن زنجیرهٔ `PARENTID` را تضمین نمی‌کند (`FK_SELFREFRENCE` فقط معلق‌نبودن را تضمین می‌کند، نه نال‌نبودن یا عمق را). پس «الان همه resolve می‌شوند» یک واقعیت دربارهٔ دادهٔ امروز است، نه یک invariant.
  - ⚠️ **قید مهم:** این verify روی دیتابیس **محیط توسعه** با حجم بسیار کم انجام شد (۱۵۰ حساب، ۵۷ سرسند، ۸۶ ردیف). نبودِ زنجیرهٔ شکسته در ۱۵۰ حساب تستی، شاهد ضعیفی برای دیتابیس عملیاتی است. **همان محدودیتی که در فاز ۱۲ باعث نتیجهٔ ناقص `VAHEDTYPE` شد.**
- **🔴 دادهٔ زندهٔ موجود تراز نیست — شاهد مستقیم و کمّی برای ریسک باز «تضمین تراز حذف شده».** (کشف‌شده حین همین verify، ۲۰۲۶-۰۹-۰۷.) sanity check مستقل روی خودِ جدول `TB_VOUCHERSDETAIL` (نه روی گزارش ما) برای سال ۱۴۰۴، فقط ردیف‌های زنده:
  - `SUM(DEBTOR) = 21,823,731,508` در برابر `SUM(CREDITOR) = 1,803,305,610` → **اختلاف ۲۰,۰۲۰,۴۲۵,۸۹۸ ریال**. کل دیتابیس در سطح سال تراز **نیست**.
  - در سطح تک‌سند: از ۳۷ سند دارای ردیف، **۲۸ سند نامتراز** و فقط ۹ سند متراز. از آن ۲۸ تا، **۲۰ تا فقط یک ردیف دارند** (یعنی ذاتاً نمی‌توانند تراز باشند) و همگی `DOCLIFE=1` (یادداشت/پیش‌نویس) اند — الگوی آشکارِ دادهٔ تستیِ نیمه‌کاره، نه فساد دادهٔ عملیاتی.
  - **پیامد عملی برای ما:** اگر روزی تصمیم بگیریم invariant تراز را در `Accounting.Application` بازسازی کنیم، **دقیقاً همان تلهٔ فاز ۱۲ تکرار می‌شود** (آنجا ۳ حساب گروه با `TYPEACTIVITY ∈ {4,5,6}` قاعدهٔ محتمل را نقض می‌کردند): دادهٔ موجود از قبل قاعده را نقض می‌کند. باید از قبل تصمیم گرفته شود که آیا اعتبارسنجی فقط روی نوشتن‌های **جدید** اعمال می‌شود یا اسناد تاریخی هم باید پاک/استثنا شوند. **حدس زده نشد.**
  - **دو خبر خوب جانبی (هر دو با کوئری تأیید شدند، نه حدس):** (۱) **صفر** ردیف در کل `TB_VOUCHERSDETAIL` هم‌زمان `DEBTOR` و `CREDITOR` غیرصفر دارد — یعنی invariant «یک‌طرفه بودن هر ردیف» با اینکه در کد enforce نمی‌شود، در دادهٔ واقعی برقرار است. (۲) **صفر** مقدار اعشاری در `DEBTOR`/`CREDITOR` وجود دارد (`x <> TRUNC(x)`) — شاهد مستقیم برای تصمیم باز «`long` در برابر `decimal?`»: Legacy عملاً ریال صحیح ذخیره می‌کند و `decimal?` ما دقتی اضافه می‌کند که داده ندارد. (هنوز اثبات نمی‌کند که ستون *نمی‌تواند* اعشار بگیرد — `NUMBER(25)` بدون scale صریح است.)
- **✅ ~~نقض قانون تیم #۲ (Read باید از View بخونه)~~ — استثنا رسماً در `CLAUDE.md` ثبت شد (یافتهٔ `/code-review`).** پیش‌تر این تصمیم فقط در کامنت XML خودِ کد توجیه شده بود، نه در سطح قانون تیم — یعنی هیچ سند بالادستی این استثنا رو تأیید نکرده بود. حالا قانون #۲ در `CLAUDE.md` صراحتاً این استثنا رو (فقط برای همین ۳ گزارش پایه) ثبت می‌کنه.
- **🟡 گزارش paging ندارد.** عمدی — تراز ناقص بی‌معناست چون باید تراز شود. ولی سطح **معین** می‌تواند هزاران ردیف برگرداند و بازهٔ تاریخ هم می‌تواند نامحدود باشد (`FromDate`/`ToDate` هر دو اختیاری‌اند). هیچ سقف/گارد روی حجم خروجی گذاشته نشد.
- **🟡 کارایی self-join سه‌سطحی روی جدول تراکنش هرگز با Execution Plan واقعی سنجیده نشد.** `TB_VOUCHERSDETAIL` جدول تراکنش است و کوئری سه self-join روی `TB_ACCOUNTCODE` (`CHAR(36)`) به‌علاوهٔ `GROUP BY` دارد. تنها ایندکس مرتبطِ تأییدشده در Fluent Mapping روی این مسیر `IDX_VOUCHERSDETAIL_HEADID` است. فیلترهای `h.YEAR`/`h.VAHEDCODE`/`h.DATE_DOC` پشتیبانی ایندکسی تأییدشده ندارند. ⚠️ این پروژه schema جدید نمی‌سازد، پس هر ایندکس پیشنهادی باید **به صاحب پروژه** منتقل شود، نه اینکه خودمان بسازیم.
- **🟢 (ثبت برای آگاهی) اولین SQL خام پروژه اینجا وارد شد.** تا فاز ۱۷ هیچ `FromSqlRaw`/`SqlQueryRaw`/Dapper در کدبیس نبود. اگر این الگو قرار است در batchهای گزارشی بعدی تکرار شود، بهتر است یک قاعدهٔ تیمی صریح برایش نوشته شود (کجا مجاز است، چطور پارامتر bind شود، و اینکه هرگز رشتهٔ ورودی کاربر وارد متن SQL نشود — در این فاز با `switch` سخت‌کدشده روی enum رعایت شد).

### 🔴 تصمیمات باز جدید — کشف‌شده در فاز ۱۷ (پاس دوم `D:\CentralAccount`، ۲۰۲۶-۰۹-۰۶)

این فاز **هیچ کدی تولید نکرد** — فقط خواندن Read-Only پروژهٔ مرجع با دانش ۲۵ Entity فازهای ۱۳–۱۶. جزئیات کامل هر مورد در `docs/centralaccount-business-reference.md` **بخش ۲۴**. طبق Accounting Safety Gate هیچ‌کدام بی‌صدا رد نشد و هیچ‌کدام حدس زده نشد.

- **🔴 فهرست ستون‌های `bool`→enum از «~۱۸ مشکوک» به «۱۶ تأییدشده» ارتقا یافت — و یک فرض پایه‌ای باطل شد.** با تطبیق مستقیم property-به-property بین Entityهای ما و `Tamin.Core` (نه از روی کامنت ستون، که فاز ۱۲ ثابت کرد غیرقابل‌اعتماد است)، هر مورد قطعی شد. **فرض باطل‌شده:** «اگر ستون واقعاً دومقداری باشد `bool` بی‌خطر است» — غلط است، چون تقریباً همهٔ این enumها از **۱** شروع می‌شوند نه از ۰، پس `false` مقدار `0` می‌نویسد که اصلاً در دامنهٔ مجاز نیست. تنها استثنا `TB_PREDESCRIB.FLAGVOUCHER` است (`Flag {0,1}`) که واقعاً با `bool?` سازگار است. جدول کامل ۲۱ ردیفی در §۲۴-۱.
  - **موارد تازه‌تأییدشده که قبلاً حتی «مشکوک» هم ثبت نشده بودند:** `TB_ATTRIBFORACCOUNTCODE.ATTRIBBOXNO` (در پروژهٔ مرجع **`int`** است — یعنی اصلاً enum نیست بلکه شمارهٔ خانهٔ صفت؛ بدترین مورد این پاس)، `TB_IDENTITYSUBGRPS.FIXED` (`IdentitySubGroupKind` ۱..۲)، `TB_ELAMHEAD.ELAMH_CASE`.
  - **موارد تأییدشدهٔ مثبت (نگاشت فعلی ما درست است):** `TB_PERSON_ACTION.STATUS`, `TB_IDENTITYSUBGRPS.SUMFLAG`, `TB_WORKSHOP.ISACTIVE` (برخلاف `TB_TAFSILI.ISACTIVE` که enum است — این دو نباید با هم اشتباه شوند), `TB_YEAR.ISCURRENT`.
  - **رفع نشد** — هر اصلاحی یک breaking change در قرارداد API است و Taskی جداست. ولی حالا برای اولین بار **فهرست کامل و قطعی** با مقادیر دقیق در دست است.

- **🔴 تفاوت بنیادین «حذف فیزیکی در برابر حذف نرم» — و ۱۲ گارد وابستگی که ما نداریم.** در پروژهٔ مرجع **۳۵ repository** حذف فیزیکی می‌کنند و فقط ۸ مورد soft delete اند؛ ما دقیقاً برعکسیم. پیامد واقعی: آنجا Oracle خودش با `ORA-02292` جلوی حذف والدِ دارای فرزند را می‌گیرد، پس مجبور بودند گارد صریح بنویسند؛ **در مدل ما هیچ FK ای شکایت نمی‌کند، پس همان نقض‌ها کاملاً بی‌صدا اتفاق می‌افتند.** گاردهایی که آن‌ها دارند و ما نداریم: `AttribForAccountCode` (استفاده در سند)، `ChequeType` (nullify وابسته‌ها)، `Expense` (cascade به لینک تفصیلی)، `IdentityGroup` (زیرگروه دارد)، `IdentitySubGroup` (فیلد ثابت/متغیر دارد)، `Rabet` (گردش مالی حساب)، `RevolvingFund` (استفاده در شارژ تنخواه)، `TafsilGroup` (لینک به حساب)، `CheckBook` (برگ چک استفاده‌شده)، `ElamHead` (وضعیت نهایی‌شده)، `PayReciveHead` (سند صادرشده)، `Tafsili` (استفاده در سند). الگوی قابل بازاستفاده: `GetTuroverByIdAsync(accountCodeId)` برای «آیا این حساب گردش دارد». **حدس زده نشد** که آیا این گاردها برای مدل حذف نرم ما هم لازم‌اند یا صرفاً پیامد حذف فیزیکی آنجا هستند — **تصمیم صاحب پروژه است.** این ریسک باز موجود «حذف گرهٔ کدینگ بدون بررسی وابستگی» (فاز ۸) و «حذف `LevelTafsil`/`TafsilGroup`» (فاز ۱۴) را تعمیم می‌دهد به ۱۲ Entity.

- **🔴 ۹ ستون که ما ورودی آزاد فراخوان گرفته‌ایم، در پروژهٔ مرجع سمت سرور تولید/ثابت می‌شوند.** یعنی API فعلی ما اجازه می‌دهد فراخوان مقادیری بنویسد که قرار بوده مشتق یا کنترل‌شده باشند: `TB_ELAMHEAD.ELAMH_SERIALNO` (سریال ۱۴ رقمی `MAX+1` به‌ازای واحد/سال/نوع)، `TB_ELAMHEAD.ELAMH_CODE` (از `TB_RABET` مشتق می‌شود)، `TB_ELAMHEAD.WEB_STAT` (ماشین حالت)، `TB_CHECKBOOK.CHECKBOOK_TYPE` (ثابت `real`)، `TB_WORKSHOP.ISACTIVE` (ثابت `true` در Create)، `TB_PERSON_ACTION.STATUS` (ثابت `true`)، `TB_PERSON_ACTION.USERNAME` (از سرویس دایرکتوری سازمان)، `RADIF` (شمارندهٔ سرور)، `TB_PAYRECIVHEAD.PAYRECIVCODE` (`PadLeft(5,'0')`). **الگوی حاکم که کشف شد:** پروژهٔ مرجع هر فیلد «وضعیت» را از Update معمولی جدا می‌کند و Command اختصاصی می‌سازد (`ChangeState`, `ReActive`, `Deactivation`, `ChangeStatusToAccept/Reviewed/Temporary`, `ChangeWebStat`). این همان چیزی است که ریسک باز 🔴 ما دربارهٔ `DOCLIFE` می‌گوید — و حالا معلوم است **قاعدهٔ عمومی** آنجاست نه استثنای سند. جزئیات §۲۴-۳.

- **🔴 `TB_PERSON_ACTION.USERID` کد ملی است، نه شناسهٔ کاربری سیستم.** پروژهٔ مرجع آن را با الگوریتم checksum کد ملی ایران (`NationalCodeValidation.CheckNationalCode(a, '0')`) + `Length(10)` اعتبارسنجی می‌کند، و `USERNAME` را **سمت سرور** از سرویس دایرکتوری سازمان می‌گیرد (`GetCurrentUserAsync(nationalCode)`؛ اگر پیدا نشد ۴۰۳). ما هر دو را رشتهٔ آزاد فراخوان می‌گیریم — یعنی **نام صاحب امضا قابل جعل است** و کد ملی نامعتبر پذیرفته می‌شود. همچنین قواعد `FROMDATE`/`TODATE` (فرمت `^\d{8}$` و `ToDate >= FromDate`) را نداریم. ⚠️ نکتهٔ مرتبط با ریسک باز `PersonAction` فاز ۱۳: کد کامل سه گارد `HasActiveRoleAsync`/`HasActiveUserAsync`/`HasActiveRoleExceptAsync` استخراج شد و معلوم شد **هیچ‌کدام `VahedCode` را فیلتر نمی‌کنند** (یعنی «یک کاربر فعال به‌ازای هر نقش در کل سیستم»). **حدس زده نشد** که این عمدی است یا باگ پروژهٔ مرجع.

- **🔴 مرز Aggregate چهار جفت Head/Detail قطعی شد — هر چهار تا composite-create اند و API ما هر چهار تا را فقط Head می‌سازد.**
  - **`CheckBook` → `TB_CHECK` (قوی‌ترین مورد و شکاف کارکردی واقعی):** `CheckBook.AddCheckPapers(from, to, userId)` **به‌ازای هر شماره در بازه یک ردیف `TB_CHECK` می‌سازد**، همه در یک تراکنش. یعنی `POST /api/check-books` ما دسته‌چکی می‌سازد که **هیچ برگ چکی ندارد** و از دید بقیهٔ سیستم خالی است. ضمناً پروژهٔ مرجع بازهٔ `From`/`To` را در Update **کاملاً تغییرناپذیر** می‌کند (هر دو شاخه throw)، ما آزادانه اجازه می‌دهیم. و اعتبارسنجی `From.Length == To.Length` را نداریم.
  - **`ElamHead` → `ElamDetail` → لینک تفصیلی:** سه‌سطحی با `BeginTransaction`/`Rollback` صریح.
  - **`PayReciveHead` → `PayReciveDetail` → لینک تفصیلی:** همان الگو + گارد شمارهٔ تکراری در محدودهٔ `(code, vahedCode, year)`.
  - **`ChargeAndCostHead` → دیتیل (هنوز CRUD نداریم):** Create یک سرصفحه + **دقیقاً یک** دیتیل.
  - این ریسک‌های باز موجود را قطعی‌تر می‌کند: «مرز Aggregate `ElamHead`/`ElamDetail`» (فاز ۱۵)، «مرز Aggregate `PayReciveHead`/`PayReciveDetail`» و «`TmpVoucherHead`/`TmpVoucherDetail`» (فاز ۱۶). **هر چهار نیازمند همان تصمیم «مدل ترکیبی» فاز ۱۰ اند. حدس زده نشد.**

- **🔴 مدل `Amount + DebCredType` — پروژهٔ مرجع تضمین «یک ردیف نمی‌تواند هم‌زمان بدهکار و بستانکار باشد» را در دو جای مستقل دارد و ما در هیچ‌جا.** (۱) در `BankCartDetail` با یک قانون XOR صریح در Validator: `(Debtor <= 0 && Creditor <= 0) || (Debtor > 0 && Creditor > 0)` → خطا. (۲) در دریافت/پرداخت با یک مدل ساختاری: دیتیل یک `Amount` + یک enum طرف (`DebCredType {Debtor=1, Creditor=2}`) می‌گیرد و Handler یکی را پر و دیگری را صفر می‌کند — یعنی نقض **ساختاراً غیرممکن** است. این مستقیماً به ریسک باز 🔴 «هیچ چیز مانع نمی‌شود یک ردیف هم‌زمان `DEBTOR` و `CREDITOR` غیرصفر داشته باشد» (فاز ۱۰) مربوط است، و حالا **دو الگوی آزموده برای رفعش در دست است**. ⚠️ ما این را نه روی `TB_BANKCARTDETAIL` (فاز ۱۵) داریم نه روی ردیف سند.

- **🔴 قاعدهٔ «تفصیلی الزامی/مجاز» — کد کامل استخراج شد و **دوطرفه** است.** فاز ۱۲ فقط مکانیزم را حدس زده بود؛ حالا کد در **سه Handler مستقل** (اعلامیه، دریافت/پرداخت، ارتقای سند موقت — و طبق فاز ۱۲ در ثبت سند هم) عیناً تکرار شده و دو قانون دارد، نه یکی: (الف) اگر سطح در `GetLevelIdByMoein(accountCodeId)` هست و مقدار داده نشده → `LevelNotProvidedException`، (ب) **اگر مقدار داده شده ولی سطح در فهرست نیست → `UnauthorizedLevelException`**. سقف سطوح ۷. منبع حقیقت `TB_ACCOUNT_LINK_LEVEL` با مکانیزم «وجود/عدم‌وجود ردیف» — **تأیید مستقل و سه‌باره**، پس نتیجه‌گیری فاز ۱۲ درست بود. ما هیچ‌کدام از دو طرف را نداریم. مرتبط با ریسک باز 🔴 «الزامی بودن تفصیلی» و با شکاف مستقیم `TB_VOUCHERDETAIL_LINK_TAFSILI.TAFSILI_ID` که FK هم ندارد.

- **🟡 ماتریس نقش‌های واقعی سازمان استخراج شد — ریسک باز IDOR حالا «شکل راه‌حل» هم دارد.** ۷ نقش در `Presentaion.Web.API/Roles/Roles.cs` (`FINANCIAL CORE SETAD ADMIN`, `… HLT ADMIN`, `… EDK ADMIN`, `… MALI ADMIN`, `… USER`, `… REPORT`, `… IT`) روی **۳۶ Controller** با `[RolesAllowed([...])]` از **همان پکیج `Tamin.Framework.Common.Security` که ما هم داریم** (فاز ۷). الگوی حاکم: خواندن → همهٔ نقش‌های عملیاتی؛ نوشتن روی دادهٔ مرجع/پیکربندی (کدینگ حساب، گروه تفصیلی، رابط، لیست سیاه/سفید) → **فقط `SETAD_ADMIN`**؛ نوشتن روی دادهٔ عملیاتی → ادمین‌های واحدی هم. ⚠️ **در خود پروژهٔ مرجع هم شکاف هست** (چند اکشن نوشتن بدون هیچ نقش، از جمله همهٔ `TmpVouchersController` شامل `Import` که سند واقعی می‌سازد) — پس کورکورانه کپی نشود. **ما `RolesAllowedAttribute` را در دسترس داریم ولی روی هیچ Endpointی استفاده نکرده‌ایم و هیچ‌جا ۴۰۳ اعلام نکرده‌ایم.** تصمیم همچنان باز است، ولی دیگر «از صفر» نیست. جزئیات §۲۴-۱۰.

- **🟡 حذف سند در پروژهٔ مرجع ۸ کار انجام می‌دهد؛ cascade سه‌سطحی فاز ۹ ما فقط ۲ تای آن را پوشش می‌دهد.** موارد غایب در ما: (۱) **`DOC_NUM` هنگام حذف به `"d" + Atf_Num.Substring(10,5)` تغییر نام می‌دهد تا شمارهٔ سند آزاد و قابل بازاستفاده شود** — با حذف نرم فعلی ما، شمارهٔ سند حذف‌شده به‌خاطر `UK_VOUCHERHEAD_NUMBER` **برای همیشه سوخته می‌ماند** (هم‌خانوادهٔ تلهٔ `UK_TBTAFSILGROUP` فاز ۱۴)؛ (۲) soft-delete ردیف‌های `TB_ATTRIBINVOUCHER` (جدولی که اصلاً به آن دست نمی‌زنیم)؛ (۳) حذف فیزیکی `TB_RECEIP` مرتبط؛ (۴) **reset برگ چک** (`CheqDate`/`PaperDesc`/`PayTo` → `null`) تا به حوضچهٔ استفاده‌نشده برگردد؛ (۵) بازکردن لینک برگشتی `TmpVoucherHead.VouchersHeadId = null`. ضمناً استراتژی حذف دیتیل آنجا **مشروط** است: `DocLife != draft` → soft delete، `draft` → حذف فیزیکی. **حدس زده نشد کدام‌یک برای ما لازم است.** قاعدهٔ شماره‌گذاری هم کشف شد: `ATF_NUM` (۱۵ رقم `vahed+year+seq`) هرگز بازاستفاده نمی‌شود، `DOC_NUM` (۶ رقم) می‌شود. جزئیات §۲۴-۷.

- **🟡 سه Entity که ما CRUD کامل ساختیم، در پروژهٔ مرجع اصلاً مسیر نوشتن ندارند.** `AccountCodeInterface` (repository فقط دو متد خواندن دارد و **حتی آن‌ها هم هیچ‌جا صدا زده نمی‌شوند** — جدول پیکربندی مدیریت‌شده مستقیماً در Oracle)، `AccountException` (فقط خوانده می‌شود؛ **معنای کسب‌وکاری‌اش کشف شد:** فهرست حساب‌های **مستثنا از سند افتتاحیه/اختتامیه**)، `BillLog` (append-only، تأیید مجدد ریسک باز فاز ۱۳). ضمناً `PreDescrib` آنجا **دقیقاً معکوس ماست**: Create + حذف فیزیکی دارد و `UpdateAsync` صریحاً `NotImplementedException` است، در حالی که ما Create + Update داریم بدون Delete. **آیا CRUD کامل روی این‌ها اشتباه بوده؟ حدس زده نشد.**

- **🟡 اعتبارسنجی‌های ساختاری‌ای که در سراسر پروژهٔ مرجع استاندارد است و ما هیچ‌کدام را نداریم.** `VAHEDCODE` همیشه `^[0-9]{4}$`؛ `YEAR` `^[0-9]{4}$` یا سخت‌گیرتر `^(13[0-9]{2}|14[0-9]{2})$`؛ تاریخ‌ها همیشه `yyyyMMdd` هشت‌کاراکتری با اعتبارسنجی واقعی تقویم شمسی؛ نام‌های فارسی با `^[؀-ۿ\s]+$`؛ کدهای دو‌رقمی `^[0-9]{2}$`. **مهم‌تر: کدینگ حساب آنجا به سه خانوادهٔ Command جدا (`Group`/`Kol`/`Moin`) شکسته شده** با قواعد طول و ساختار متفاوت (۲/۴/۶ رقم، هیچ بخش دو‌رقمی `"00"` نباشد، `ParentId` اجباری برای کل و معین) — ما یک `CreateAccountCodeCommand` عمومی داریم که هیچ‌کدام را چک نمی‌کند. برای `Tafsili` (CRUD آینده) هم checksum کد ملی ۱۰ رقمی / شناسهٔ ملی ۱۱ رقمی اعمال می‌شود. **هیچ‌کدام بازسازی نشد** (طبق «Legacy جایگزین کامل»، بازسازی invariant نیاز به تصمیم صریح دارد) — ولی حالا فهرستشان موجود است. جزئیات §۲۴-۱۱.

- **🟡 دو باگ در گزارش‌های تراز آزمایشی پروژهٔ مرجع که نباید تکرار کنیم.** (۱) فیلتر `h.isdeleted = 0` فقط روی حالت‌های `moin` و `kol` هست و در حالت‌های `group` و `Tafsili` **وجود ندارد** → اسناد حذف‌شده در آن دو سطح شمرده می‌شوند. (۲) `d.isdeleted` **هیچ‌جا** فیلتر نمی‌شود → ردیف‌های حذف‌شدهٔ سند همیشه وارد جمع می‌شوند. برای ما بحرانی‌تر است چون `DeleteVoucherDetailCommand` مستقل (فاز ۱۰) ردیف را بدون لمس سرصفحه حذف می‌کند. ⚠️ در همین بررسی، **سومین تأیید مستقل برای نگاشت `TYPECODE {Group=1, Kol=2, Moin=3}`** پیدا شد (`WHERE a.typecode = 3` برای معین، و پیمایش کل/گروه فقط با self-join روی `PARENTID`)، و **شاهد قوی جدید برای ترتیبی‌بودن `DOCLIFE`**: فیلتر گزارش‌ها `h.doclife >= :DocLife` است نه `=` — با `bool` مفهوم «حداقل بررسی‌شده» اصلاً قابل بیان نیست.

- **🟡 ابهام سیستماتیک جدید — جابه‌جایی `Debtor`/`Creditor` در هر دو مسیر تبدیل به سند.** هم `AddVochersImportTempCommandHandler` (سند موقت → سند) و هم `AddVoucherByPayAndReciveCommandHandler` (دریافت/پرداخت → سند) عیناً می‌نویسند `SetDebtor(detail.Creditor)` و `SetCreditorr(detail.Debtor)`. دو مسیر مستقل، هر دو معکوس. یا معنای این دو ستون در جدول‌های staging/دریافت‌پرداخت نسبت به سند وارونه است، یا هر دو یک باگ کپی‌شده‌اند. **حدس زده نشد** — ولی اگر روزی مسیر تبدیل بسازیم، این اولین سؤالی است که باید از صاحب پروژه پرسیده شود. ابهام مشابه: توزیع معکوس مبلغ بر اساس `ElamCase` در ساخت اعلامیه.

- **🟡 `TB_VOUCHERSHEAD.PARENTHEAD_ID` خودارجاع نیست.** در پروژهٔ مرجع به `TmpVoucherHead.Id` یا `PayReciveHead.Id` اشاره می‌کند («این سند از کدام منبع تولید شد»)، نه به سند دیگر. قانون `ParentHeadId != Id` که فاز ۸ اضافه کرد بی‌ضرر است ولی موضوعیت ندارد. (در `LegacyDbContext` ما این ستون هیچ FK ندارد، پس سازگار است.)

- **✅ یافتهٔ منفی مفید — در بُعد cross-cutting ما جلوتریم.** `ApplicationUseCases/Registration.cs` پروژهٔ مرجع فقط سه کار می‌کند (AutoMapper، ثبت Validatorها، `ValidatorBehavior`) و **هیچ Behavior دیگری وجود ندارد** — نه logging، نه transaction، نه multi-tenancy. فرضیهٔ «شاید الگوهای cross-cutting داشته باشند که ما نداریم» **رد شد**. ضمناً `IBusinessUserAccess.UserId` آنجا یک property قابل‌نوشتن است، یعنی مکانیزم Audit ذاتاً ضعیف‌تر از `ICurrentUser` fail-loud ماست. تنها چیز واقعی آن‌طرف، متد `HaveAccessToUnit` است که دسترسی را **سلسله‌مراتبی** از `TB_VAHED_INFO.PARENT_ID` محاسبه می‌کند — ولی فقط در **۸ نقطه از ۳۷۲ Handler** صدا زده می‌شود، که درس تکراری «باید `Behavior` سراسری باشد» را با عدد دقیق‌تر تأیید می‌کند.

