# لاگ پیشرفت پروژه

هر روز یک خط جدید (تاریخ + خلاصه کوتاه کار انجام‌شده) به این فایل اضافه کن.

- 2026-08-19 (ادامهٔ همان جلسه، پیش از commit): **حذف کامل `PUT`/`DELETE` از کل API به درخواست صریح صاحب پروژه.** ایشان اعلام کردند که در این محیط فقط `GET` و `POST` قابل استفاده‌اند (دلیل فنی را نگفتند؛ احتمالاً محدودیت زیرساخت شبکه/سرور سازمان) — این به‌عنوان یک قاعدهٔ داده‌شده پذیرفته و پیاده شد، نه یک انتخاب معماری داخلی. هر ۴ Endpoint نوشتنِ فاز ۸ بازنویسی شدند: `PUT /api/account-codes/{id}` → **`POST /api/account-codes/{id}/update`** و `DELETE /api/account-codes/{id}` → **`POST /api/account-codes/{id}/delete`** (و معادل‌هایشان برای `voucher-heads`). **دو تصمیم قرارداد که ثبت شد:** (۱) **فعل صریح در route** انتخاب شد نه `POST /api/account-codes/{id}` خالی، چون `POST /api/account-codes` (ساخت) از قبل وجود دارد و مسیر بدون فعل در کنارش «ساخت زیرمنبع تحت این id» خوانده می‌شود؛ حالا که از semantics استاندارد REST خارج شده‌ایم صراحت بهتر از ابهام است و مسیر برای Endpointهای بعدی (`/{id}/lines`) هم باز می‌ماند. (۲) **status موفقیت از 204 به 200 + بدنهٔ `{id}`** تغییر کرد، چون همان محدودیت زیرساختی که `PUT`/`DELETE` را ممنوع کرده نشان می‌دهد لایه‌های میانی شبکه رفتار غیرمعمول دارند و ۲۰۰ با بدنهٔ کوچک کمتر از ۲۰۴ خالی مستعد ابهام است؛ ضمناً با `POST` ساخت که از قبل بدنه برمی‌گرداند هم‌خانواده می‌شود و `id` برگشتی تأیید می‌کند کدام رکورد تحت تأثیر قرار گرفته. چهار record پاسخ جدید (`UpdateAccountCodeResponse` و…) هم‌سبک `CreateAccountCodeResponse` اضافه شد. **این تغییر فقط لایهٔ Api را لمس کرد** — Command/Handler/Validator/Repository/`GlobalExceptionHandler` همگی دست‌نخورده ماندند (Controller خودش `id` را از route دارد و بدنهٔ پاسخ را می‌سازد)، پس منطق soft delete، idempotency، 404 مرکزی و audit سمت سرور بدون تغییر باقی ماند. `GET` (لیست و by-id) و `POST` ساخت هم دست نخوردند. **قاعده با تست قفل شد:** `HttpVerbConventionTests` جدید با reflection روی کل assembly تأیید می‌کند **هیچ اکشنی در هیچ Controllerی** `[HttpPut]` یا `[HttpDelete]` ندارد، پس بازگشت تصادفی این قاعده در آینده تست را قرمز می‌کند (نه اینکه بی‌صدا از قلم بیفتد). XML doc هر ۴ اکشن و سطح کلاس هر دو Controller صریحاً توضیح می‌دهد که این انحراف از REST **درخواست صاحب پروژه** بوده تا آیندهٔ پروژه گیج نشود. build ۰ خطا / هیچ warning نوع CS، **۲۵۵/۲۵۵ تست سبز** (۲۲ Domain + ۱۵۳ Application + ۶۰ Api + ۲۰ Infrastructure) — مستقلاً توسط ارکستریتور اجرا و تأیید شد. `CLAUDE.md`، `ROADMAP.md` و GitHub Project به‌روزرسانی شدند. **commit/push طبق دستور صریح انجام نشد** — منتظر review نهایی روی کل diff و تأیید صاحب پروژه.

- 2026-08-19: **فاز ۸ — تکمیل CRUD: Update + Delete (Soft Delete)** (برنچ `changejWT`). تا پیش از این فقط Create (فاز ۵) و Read (فاز ۶) وجود داشت؛ حالا هر دو Entity چرخهٔ کامل CRUD دارند. `backend-dotnet` چهار Command ساخت (`UpdateAccountCode`/`DeleteAccountCode`/`UpdateVoucherHead`/`DeleteVoucherHead`) با Handler/Validator، به‌همراه ۴ Endpoint: `PUT`/`DELETE` روی `/api/account-codes/{id:guid}` و `/api/voucher-heads/{id:guid}`، همگی موفق → **204**. **تصمیم PUT به‌جای PATCH** (تصمیم صریح، نه پیش‌فرض): تقریباً همهٔ ستون‌های Legacy nullable اند (`bool?`/`Guid?`/`string?`)، پس در PATCH جزئی «فیلد ارسال‌نشده» از «فیلد صریحاً `null`» تفکیک‌پذیر نیست مگر با wrapper نوع `Optional<T>` روی تک‌تک فیلدها — که هم قانون «Command فقط primitive دارد» را می‌شکند و هم ماشین‌آلات نامتناسبی اضافه می‌کند؛ ضمناً PUT با شکل `CreateXCommand` قرینه است. پیامد پذیرفته‌شده: بدنهٔ کامل الزامی است و فیلد جاافتاده یعنی `null` شدن ستون. **`Id` از route می‌آید نه بدنه** — برای PUT یک record جدا در لایهٔ Api (`UpdateAccountCodeRequest`/`UpdateVoucherHeadRequest`) تعریف شد که **اصلاً پراپرتی `Id` ندارد**، پس کل کلاسِ باگِ «تناقض id مسیر با id بدنه» ساختاراً ناممکن است (این ضمناً اولین اجرای عملی پیشنهاد باز `api-contract` برای جداکردن Request از Command است، فعلاً فقط روی PUT). **Audit فقط سمت سرور** — `CHANGEUSERID = _currentUser.UserId` و `UPDATEDDATE = DateTime.UtcNow`، دقیقاً الگوی فاز ۷؛ هیچ پراپرتی userId در هیچ‌کدام از ۴ Command نیست پس بازگشت جعل Audit **خطای کامپایل** می‌دهد. ستون‌های `ID`/`ADDUSERID`/`CREATEDDATE`/`ISDELETED` (و `GLOBALNUMBER`/`ATTACHFILE` برای سند) در Update وجود ندارند و لمس نمی‌شوند — با تست صریح قفل شد؛ `ISDELETED` عمداً بیرون ماند چون در غیر این صورت Update به در پشتی delete/undelete تبدیل می‌شد. **Soft Delete:** فقط `ISDELETED = true` + audit؛ هیچ `Remove`/`ExecuteDelete` در کد نیست و چون write repositoryها اصلاً متد حذف **ندارند**، حذف فیزیکی از این مسیر ساختاراً ناممکن است (`qa-tester` این را با تست reflection روی interface قفل کرد). **404 مرکزی:** `NotFoundException` جدید → `GlobalExceptionHandler` → 404 `ProblemDetails`، دقیقاً به الگوی `DuplicateKeyException` → 409؛ با تست اثبات شد پیام exception (شامل نام منبع و id) به بدنهٔ پاسخ **درز نمی‌کند**. **Repository:** فقط یک متد `GetForUpdateAsync` اضافه شد که **عمداً tracked است** (بدون `AsNoTracking()`، برخلاف read repository)؛ Repository همچنان هرگز `SaveChangesAsync` نمی‌زند و **`IUnitOfWork` هیچ تغییری نکرد** — دقیقاً همان‌طور که در فاز ۵ طراحی شده بود. **رفتار مرزی تست‌شده:** Update روی رکورد ناموجود یا `ISDELETED == true` → 404؛ Delete روی ناموجود → 404؛ Delete روی رکورد از قبل حذف‌شده → **204 بدون هیچ نوشتنی** (idempotent طبق HTTP، `SaveChangesAsync` اصلاً صدا زده نمی‌شود و audit قبلی دست‌نخورده می‌ماند)؛ و ظریف‌ترین حالت: `ISDELETED == null` هم «حذف‌نشده» است پس واقعاً soft-delete می‌شود و idempotent تلقی نمی‌شود. خطای UNIQUE در PUT از همان مسیر موجود (`UnitOfWork` → `DuplicateKeyException` → 409) عبور می‌کند بدون هیچ pre-check دستی — که برای شرایط رقابتی رفتار درستی است. **یافتهٔ `api-contract`: 401 در هیچ‌کدام از Endpointها اعلام نشده بود** — نه ۴ تای جدید نه ۶ تای قبلی — با اینکه همگی زیر `SetFallbackPolicy(RequireAuthenticatedUser)` اند؛ برای هر ۱۰ Endpoint یکدست اضافه شد. **403 عمداً اعلام نشد** چون هیچ authorization مبتنی بر نقش وجود ندارد و اعلامش گمانه‌زنی می‌بود. `qa-tester` **۸۰ تست جدید** نوشت (۶۹ Application + ۱۱ Api)؛ build ۰ خطا / ۱۶ warning پیش‌موجود NU1903 (هیچ CS)، **۲۵۰/۲۵۰ تست سبز** (۲۲ Domain + ۱۵۳ Application + ۵۵ Api + ۲۰ Infrastructure)، **صفر رگرسیون** — که به‌صورت مستقل توسط ارکستریتور هم اجرا و تأیید شد. همگی Unit/Mock، **هیچ اتصالی به Oracle زنده**. **۶ تصمیم باز جدید ثبت شد و عمداً حل نشد** (طبق Accounting Safety Gate بی‌صدا رد نشدند): (۱) 🔴 **IDOR حالا به مسیر نوشتن رسید** — تا فاز ۷ فقط خواندن بود، حالا هر کاربر احراز‌شده می‌تواند هر حساب/سندی را با دانستن `ID` ویرایش یا حذف کند، از جمله واحدهای دیگر؛ شدت از «افشای اطلاعات» به «تغییر/حذف غیرمجاز دادهٔ حسابداری» ارتقا یافت (مسدودکنندهٔ استقرار)؛ (۲) 🔴 **حذف سند cascade ندارد** — ردیف‌های `TB_VOUCHERSDETAIL` و تفصیلی‌هایشان دست‌نخورده و فعال می‌مانند؛ (۳) 🔴 **حذف گرهٔ کدینگ بررسی وابستگی ندارد** — فرزندان یتیم ولی فعال می‌مانند و چون حذف نرم است FK دیتابیس شکایت نمی‌کند پس ناسازگاری کاملاً بی‌صدا است؛ (۴) 🔴 **سند Post شده حالا واقعاً قابل تغییر است** — `DOCLIFE` در PUT آزاد است؛ این invariant در تصمیم دوم آگاهانه حذف شده بود ولی تا پیش از این مسیر نوشتن فقط `INSERT` داشت پس قابل بهره‌برداری نبود؛ (۵) 🟡 **نبود کنترل همزمانی** — schema Legacy `rowversion` ندارد، رفتار last-write-wins است؛ (۶) 🟡 **نبود مسیر undelete/restore**. `CLAUDE.md`، `ROADMAP.md` و GitHub Project به‌روزرسانی شدند. **commit/push طبق دستور صریح کاربر انجام نشد** — منتظر تأیید خودش.

- 2026-08-18: **فاز ۶ — اولین لایهٔ HTTP پروژه** (برنچ `GetAccountCode`). به درخواست صریح صاحب پروژه («برای هر سرویس که اضافه کردی، چه command چه query، تا مرحله کنترلر پیش ببر») هر ۶ سرویس موجود (۲ Command + ۴ Query) به Endpoint تبدیل شدند. `backend-dotnet` دو Controller ساخت: `AccountCodesController` (`/api/account-codes`) و `VoucherHeadsController` (`/api/voucher-heads`) — نام «voucher-heads» عمداً به‌جای «vouchers» انتخاب شد تا ردیف‌های سند بعداً بدون rename زیر `/{id}/lines` بیایند. هر Controller: `POST` → **201** + هدر `Location` + `{id}`؛ `GET` لیست با `pageNumber`/`pageSize` از query string (پیش‌فرض ۱ و ۲۰، سقف `MaxPageSize=200`)؛ `GET /{id:guid}` → **404** وقتی Handler مقدار `null` برمی‌گرداند. Controllerها **کاملاً نازک**اند: فقط `_mediator.Send(...)` و تنها انشعاب مجاز `null → 404`؛ هیچ `try/catch` و هیچ business logic. **تأیید شد `ValidationBehavior` روی Query هم فعال است** (چون generic روی `IRequest<TResponse>` است، نه فقط Command). **تصمیم باز «خطای UNIQUE خام» حل شد:** چون Controller این خطا را مستقیم به کاربر برمی‌گرداند، دیگر قابل تعویق نبود — `UnitOfWork.SaveChangesAsync` (تنها جای مجاز برای شناختن Oracle) الگوی `DbUpdateException { InnerException: OracleException { Number: 1 } }` را می‌گیرد و به `DuplicateKeyException` سطح Application ترجمه می‌کند، و `GlobalExceptionHandler` مرکزی (`IExceptionHandler` + `AddProblemDetails`) آن را به **409 Conflict** با پیام عمومی نگاشت می‌کند؛ نگاشت‌های دیگر: `ValidationException` → **400** `HttpValidationProblemDetails` با دیکشنری فیلد→پیام، سایر → **500** عمومی. **`Accounting.Api` هیچ ارجاع تایپی به Oracle ندارد** (فقط در XML doc نام برده شده) و `Accounting.Domain` همچنان صفر `PackageReference` دارد — Architecture Guard بررسی و تأیید شد. `api-contract` تأیید کرد هر status code اعلام‌شده واقعاً قابل‌دسترس است و برعکس، و یک شکاف واقعی یافت و بست: `GenerateDocumentationFile` + `IncludeXmlComments` فعال نبود، پس XML docهای غنی اصلاً در Swagger دیده نمی‌شدند. `qa-tester` پروژهٔ `backend/tests/Accounting.Api.Tests` را با **۲۴ تست** ساخت (بدون هیچ اتصال Oracle) — شامل اثبات ۴۰۴ روی `null`، فوروارد شدن `CancellationToken`، و تست اینکه **بدنهٔ JSON سریال‌شدهٔ پاسخ ۵۰۰ هیچ stack trace یا متن Oracle/SQL ندارد**؛ برای تست ترجمهٔ ORA-00001 یک `OracleException` **واقعی** از طریق reflection روی constructor داخلی درایور ساخته شد (نه stub) و ۴ سناریو پوشش داده شد از جمله اینکه ORA-00904 عیناً rethrow می‌شود. build ۰ خطا / ۱۶ warning پیش‌موجود NU1903، **۱۳۱/۱۳۱ تست سبز** (۲۲ Domain + ۸۵ Application + ۲۴ Api). **Gate امنیتی دو یافتهٔ CRITICAL برگرداند که هیچ‌کدام حل نشد و به‌عنوان تصمیم باز ثبت شدند:** (۱) پروژه **هیچ Authentication/Authorization ای ندارد** — `Program.cs` فقط `UseAuthorization()` را بدون هیچ scheme صدا می‌زند، پس هر فراخوان بی‌نام‌ونشان می‌تواند در دیتابیس حسابداری واقعی ردیف درج کند و کل کدینگ و اسناد را بخواند؛ (۲) `AddUserId` در هر دو Command **از بدنهٔ HTTP** گرفته و مستقیم در ستون Audit نوشته می‌شود، که **تنها تضمین حسابداری باقی‌ماندهٔ پروژه (Audit Trail) را باطل می‌کند** — راه‌حل پیشنهادی: حذف از Command و جایگزینی با `ICurrentUser` سمت سرور. چهار ریسک 🟡 دیگر هم ثبت شد (`VAHEDCODE` مرز ایزولاسیون نیست، فیلدهای Audit در DTO عمومی‌اند، نبود Rate Limiting، بدنهٔ POST همان MediatR Command است، نبود نسخه‌بندی API). `CLAUDE.md` و `ROADMAP.md` به‌روزرسانی شدند. **commit طبق دستور صریح کاربر زده نشد** — منتظر تأیید خودش.

- 2026-08-18: **حل ریسک 🔴 `PARENTID DEFAULT '0'`** (همان برنچ `addAccountCode`). `HasDefaultValueSql("'0'")` از Fluent Mapping `TB_ACCOUNTCODE.PARENTID` حذف شد؛ فقط `HasConversion(GuidToChar36Converter)` باقی ماند. صاحب پروژه با توضیح قاعدهٔ کدینگ Legacy تأیید کرد که این ستون باید بتواند واقعاً `NULL` باشد: گروه بدون والد (`PARENTID = null`)، کل والدش گروه، معین والدش کل (مثال عددی: گروه `11` → کل `1101` → معین `110101`). با حذف `HasDefaultValueSql`، EF Core دیگر `PARENTID` را `ValueGenerated.OnAdd` نمی‌داند، پس همیشه مقدار CLR واقعی (شامل `null` برای حساب ریشه) را صریح در INSERT می‌فرستد به‌جای حذف ستون و تکیه به DEFAULT ناسازگار DB. DDL خود Oracle دست‌نخورده ماند — فقط نگاشت سمت کد. build ۰ خطا، **۶۳/۶۳ تست سبز**. ۸ ستون دیگر با ریسک مشابه (`sys_guid()` در ۸ جدول) هنوز حل‌نشده‌اند چون هیچ Command هنوز به آن‌ها دست نزده. commit/push طبق تأیید صریح کاربر انجام شد.
- 2026-08-18: **فاز ۵ — اولین مسیر نوشتن CQRS واقعی پروژه** (برنچ `addAccountCode`، از روی `feature/legacy-entity-guid-ids`). طبق ترتیب صریح صاحب پروژه ساخته شد: **UnitOfWork → Interface → Repository → Command Service**. `IUnitOfWork` (در `Accounting.Application/Common/Interfaces/`) عمداً **باریک و entity-agnostic** طراحی شد — فقط `SaveChangesAsync` + `Begin/Commit/RollbackTransactionAsync` و **هیچ پراپرتی per-entity** — تا افزودن Entity بعدی صرفاً یک Interface + Repository + Command بخواهد و هرگز نیاز به دست‌زدن به خود UnitOfWork نباشد. **محل Interfaceها `Accounting.Application` انتخاب شد نه Domain**، چون قید «`Accounting.Domain` صفر وابستگی خارجی» معکوس نشده و Domain باید POCO خالص بماند (تأیید شد: `Accounting.Domain.csproj` همچنان صفر `PackageReference` دارد و هیچ ارجاع EF/Oracle در Application یا Domain نیست). **مرز تراکنش در Handler** است: Repository فقط `DbSet.AddAsync` می‌زند و هرگز `SaveChangesAsync` صدا نمی‌زند؛ Handler دقیقاً یک‌بار `SaveChangesAsync` می‌زند (این ترتیب حالا با تست صریح قفل شده). **Command هرگز Entity نیست** — فقط primitive، و Handler خودش Entity می‌سازد و `ID = Guid.NewGuid()` می‌گذارد (تأیید شد `ID` هیچ‌کدام از دو جدول DB-generated نیست). اعتبارسنجی عمداً فقط سطحی است (`NotEmpty`/`MaximumLength` دقیقاً منطبق با Fluent Mapping) و طبق تصمیم «Legacy جایگزین کامل» **هیچ invariant حسابداری بازسازی نشد**. دو Command ساخته شد: `CreateAccountCodeCommand` (`TB_ACCOUNTCODE`) و `CreateVoucherHeadCommand` (`TB_VOUCHERSHEAD`) به‌همراه Handler و Validator، `ValidationBehavior` به‌عنوان MediatR pipeline، و `DependencyInjection` در هر دو لایه. `LegacyDbContext` با `UseOracle(configuration.GetConnectionString("DefaultConnection"))` ثبت شد؛ **هیچ رمزی در هیچ فایلی نوشته نشد** — `appsettings.json` فقط یک placeholder خالی دارد و مقدار واقعی از User Secrets می‌آید. پکیج‌های جدید: `MediatR` 14.2.0 + `FluentValidation` 12.1.1 (+DI extensions) در Application، `Moq` 4.20.72 در تست. `qa-tester` پروژهٔ `backend/tests/Accounting.Application.Tests` را با **۴۱ تست Unit کاملاً Mock-based** ساخت (عمداً **بدون هیچ اتصال به Oracle واقعی**، برای جلوگیری از side effect روی دیتابیس Legacy). build ۰ خطا / ۱۶ warning پیش‌موجود NU1903 (هیچ CS)، **۶۳/۶۳ تست سبز** (۲۲ Domain + ۴۱ Application). **ریسک 🔴 جدید کشف شد:** `TB_ACCOUNTCODE.PARENTID` هم‌زمان `HasDefaultValueSql("'0'")` و `GuidToChar36Converter` دارد؛ چون DEFAULT در EF Core ضمناً `ValueGenerated.OnAdd` می‌سازد، ساخت **حساب ریشه** (`ParentId = null`) باعث می‌شود EF ستون را از INSERT حذف کند و Oracle `'0'` بنویسد → یا نقض FK `FK_SELFREFRENCE` یا ردیفی که هر خواندن بعدی‌اش `FormatException` می‌دهد. این دقیقاً همان ریسک ۹ ستون DEFAULT است که قرار بود «قبل از اولین Command» حل شود. **نیازمند تصمیم کاربر.** دو ریسک 🟡 دیگر هم ثبت شد: نبود نگاشت خطای UNIQUE (`UK_ACCOUNTCODE`، `UK_VOUCHERHEAD_NUMBER` — که خبر خوبش این است که تضمین «شماره سند تکراری نشود» در سطح DB وجود دارد)، و وجود دو UserSecrets store رقیب با کلید یکسان. **هیچ Controller/Query/Update/Delete ساخته نشد** (عمداً خارج از دامنه). commit و push طبق دستور صریح کاربر انجام **نشد** — منتظر تأیید او.

- 2026-08-17: **حذف فیزیکی مدل Rich به درخواست صریح کاربر** (پس از `git init` + commit اولیهٔ `9f760ad` که حذف را قابل‌بازگشت کرد). **۲۲ فایل حذف شد:** ۹ Entity + `VoucherPostingValidator` + `DetailRequirement` + `SubsidiaryDetailPolicy` + ۳ فایل Exception که پس از حذف بالا هیچ مصرف‌کننده‌ای نداشتند (`VoucherExceptions.cs` ۴ کلاس، `VoucherLineExceptions.cs` ۴ کلاس، `DetailTypeExceptions.cs` ۱ کلاس) + ۷ فایل تست وابسته (شامل `TestSupport/DomainFactory.cs` و `EntityIdAssigner.cs`). `DuplicateAccountCodeException` به‌صورت جراحی از داخل `CodingExceptions.cs` حذف شد چون دو کلاس دیگر آن فایل (`InvalidAccountCodeException` توسط `AccountCode`، `InvalidTitleException` توسط `Guard`) هنوز استفاده می‌شوند. پوشه‌های خالی `Entities/`, `Rules/`, `tests/Entities/`, `tests/Rules/`, `tests/TestSupport/` حذف شدند. crefهای معلق در XML doc فایل `AccountCode.cs` اصلاح و `NoWarn CS0618` از csproj تست برداشته شد. **باقی‌مانده:** `Money`, `AccountCode`, `AccountNature`, `VoucherStatus`, `Guard`, `DomainException`, `MoneyExceptions`, `CodingExceptions`. build ۰ خطا / ۱۶ warning پیش‌موجود (هیچ warning نوع CS) و **۱۲/۱۲ تست سبز** (از ۳۳؛ ۲۱ تست مربوط به تایپ‌های حذف‌شده بود). ⚠️ پوشش تست مدل نوشتن مبتنی بر Legacy فعلاً صفر است. commit طبق درخواست کاربر زده نشد.
- 2026-08-17: **تصمیم معماری دوم کاربر — «Legacy کاملاً جایگزین شود» (گزینهٔ ج).** پس از ارائهٔ تحلیل ریسک، کاربر آگاهانه پذیرفت که invariantهای مدل Rich (تراز سند، immutability پس از Post، `Requirement` تفصیلی، سلسله‌مراتب ثابت) کنار گذاشته شوند. اجرا به‌صورت **منسوخ‌سازی، نه حذف فیزیکی**: ۱۲ تایپ (۹ Entity + `VoucherPostingValidator` + `DetailRequirement` + `SubsidiaryDetailPolicy`) با `[Obsolete]` سطح warning علامت خوردند؛ `Money`/`AccountCode`/`AccountNature`/`VoucherStatus`/`Guard`/`Exceptions` دست‌نخورده ماندند؛ `NoWarn CS0618` به csproj تست اضافه شد. **هیچ فایلی حذف نشد** و ۳۳ تست سبز ماندند (اکنون «مستندسازی اجرایی رفتار منسوخ»، نه اثبات مدل نوشتن فعلی). `docs/chart-of-accounts.md` با بنر SUPERSEDED علامت خورد ولی پاک نشد. فایل‌های قانون `team-lead.md` (Accounting Safety Gate به جدول «شکاف‌های شناخته‌شده» تبدیل شد) و `accounting-domain.md` (بخش «قوانین کلیدی») با تصمیم جدید هماهنگ شدند. **ابهام منبع حقیقت تفصیلی حل شد:** `TB_ACCOUNT_LINK_TAFSILGROUP` (FK `FK_TAFSILGOUP_ACCOUNTCODE` به `TB_ACCOUNTCODE` + UNIQUE `UK_ACCOUNTLINKTAFSILGROUP` روی `ACCOUNT_ID, LEVEL_ID, TAFSILGROUP_ID`) منبع حقیقت است؛ `TB_ACCOUNT_LINK_TAFSILI` علی‌رغم نامش به حساب **بانکی** (`TB_ACCOUNT`) وصل است و به کدینگ ربطی ندارد. build ۰ خطا / ۱۶ warning پیش‌موجود، ۳۳/۳۳ تست سبز. مخزن git توسط کاربر init شد؛ commit طبق درخواست خود کاربر انجام **نشد**.
- 2026-08-17: **تغییر معماری به درخواست صریح صاحب پروژه — Legacy-as-Domain.** قانون قبلی («Entityهای Legacy الزاماً Domain Entity نیستند») معکوس شد. فایل‌های قانون ایجنت اصلاح شدند: `team-lead.md` (اصل معماری + Agent Registry + Architecture Guard)، `database-reverse-engineer.md` (بخش «اصل مهم»، مسیر خروجی Scaffold، «ممنوع»، Workflow)، `entity-mapper.md` (بازتعریف کامل نقش از «جلوگیری از نشت Legacy به Domain» به «ادغام کنترل‌شدهٔ Legacy در Domain»)، `backend-dotnet.md` و `accounting-domain.md`. سپس هر ۶۵ Entity از `Accounting.Infrastructure/Legacy/Entities/` به `Accounting.Domain/Legacy/Entities/` منتقل و namespace به `Accounting.Domain.Legacy` تغییر کرد؛ `LegacyDbContext` + Fluent Mapping در Infrastructure ماند (جهت وابستگی Infrastructure → Domain؛ Domain همچنان صفر وابستگی خارجی). build ۰ خطا / ۱۶ warning پیش‌موجود، ۳۳/۳۳ تست سبز. **مدل Rich ادغام نشد** — تحلیل `accounting-domain` نشان داد جایگزینی آن با ساختار Legacy invariantهای بحرانی (تراز سند، immutability سند Post شده، فیلد `Requirement` تفصیلی، تفکیک سه سطح کدینگ) را از بین می‌برد؛ تصمیم به کاربر escalate شد. commit انجام نشد (git init نشده).
- YYYY-MM-DD: راه‌اندازی اولیه ریپازیتوری و تیم ایجنت‌ها
- 2026-08-17: Reverse Engineering دیتابیس Legacy Oracle (schema `CENTRALACCOUNT`): کشف Read-Only کامل (۶۵ جدول / ۷۷۴ ستون / ۸۲ FK / ۲۹ UNIQUE / ۱ sequence / ۲۸ View) و سپس Scaffold همهٔ ۶۵ جدول به Legacy Entity + Fluent Mapping در `Accounting.Infrastructure/Legacy/` (۶۵ Entity + `LegacyDbContext`). پکیج‌های `Oracle.EntityFrameworkCore` و `Microsoft.EntityFrameworkCore.Design` به Infrastructure اضافه شد (Domain همچنان بدون هیچ پکیج). build موفق (۰ خطا) و ۳۳ تست دامنه همچنان سبز. هیچ DML/DDL روی Legacy، هیچ secret در کد. commit انجام نشد (git init نشده).
- 2026-08-17: طراحی و پیاده‌سازی نهایی مدل دامنهٔ کدینگ شناور در `Accounting.Domain` (Entities/ValueObjects/Rules + سلسله‌مراتب Exceptionها، بدون هیچ وابستگی خارجی)؛ اعتبارسنجی دولایه (زمان ساخت ردیف + `VoucherPostingValidator` در زمان Post)؛ تکمیل کامل `docs/chart-of-accounts.md`؛ ساخت `backend/tests/Accounting.Domain.Tests` با ۳۳ تست واحد — همگی Passed (۰ خطا). هیچ باگ دامنه‌ای یافت نشد. commit انجام نشد (مخزن git هنوز init نشده).
- 2026-08-16: راه‌اندازی اسکلت solution بک‌اند (Domain/Application/Infrastructure/Api روی net10.0) با رفرنس‌های Clean Architecture و Swagger فعال روی `/swagger`؛ build بدون خطا و endpointهای `/swagger/index.html`، `/swagger/v1/swagger.json` و `/api/health` توسط qa-tester با کد ۲۰۰ تأیید شدند. مخزن git هنوز init نشده، پس commit انجام نشد.
- 2026-08-18: **تبدیل شناسه‌های Legacy از `string` به `System.Guid`** (برنچ `feature/legacy-entity-guid-ids`). دامنهٔ کار مکانیکی و قابل‌ممیزی تعیین شد: دقیقاً ستون‌هایی که در Fluent Mapping با `HasMaxLength(36) + IsFixedLength()` (یعنی Oracle `CHAR(36)`) نگاشت شده بودند → **۱۷۷ پراپرتی در ۶۴ از ۶۵ Entity** (۱۱۵ `Guid` + ۶۲ `Guid?`). **پیش از هر تغییری** `database-reverse-engineer` روی دادهٔ زندهٔ `CENTRALACCOUNT` یک sweep کاملاً Read-Only زد و **۱۲۴٬۰۹۴** مقدار غیر-null را full-scan کرد: ۱۰۰٪ GUID استاندارد dashed و **lowercase**، صفر مقدار خراب، صفر اختلاف طول/padding؛ صحت ارجاعی هم با join حساس‌به‌حروف تأیید شد (`SYSTEM_TYPE→TB_SYSTYPE` ۵۷/۵۷، `PARENTID→ID` ۱۲۲/۱۲۲) — یعنی تبدیل بر پایهٔ داده تأیید شد، نه حدسِ نام‌گذاری. ستون فیزیکی Oracle **تغییر نکرد** (هیچ DDL/DML)؛ تبدیل با `GuidToChar36Converter` (یک `ValueConverter<Guid,string>` مشترک، write با `ToString("D").ToLowerInvariant()` و read با `ParseExact(s.Trim(),"D")`) در `Accounting.Infrastructure/Legacy/` انجام می‌شود و صریحاً روی تک‌تک ۱۷۷ پراپرتی اعمال شد (نه Convention سراسری). `Accounting.Domain` همچنان **صفر** وابستگی خارجی دارد. ستون‌های `ID`دار ولی غیر-GUID عمداً `string` ماندند (`ADDUSERID`/`CHANGEUSERID`/`USERID` طول ۱۰، `DATE_RSID` طول ۸، `CONTROLID` عددی، `VAHEDCODE`/`ACCOUNTNUMBER`). نکتهٔ ظریف: `TB_VOUCHERSHEAD.SYSTEM_TYPE` بدون پسوند `_ID` ولی `CHAR(36)` و FK واقعی است و تبدیل شد. **ریسک جدید کشف شد (🔴):** ۹ ستون DEFAULT سمت DB دارند که GUID معتبر تولید نمی‌کنند — `TB_ACCOUNTCODE.PARENTID` با `DEFAULT '0'` و ۸ جدول با `ID DEFAULT sys_guid()` (که `RAW(16)` را بدون dash و UPPERCASE می‌نویسد)؛ در دادهٔ فعلی هیچ نمونه‌ای ندارند ولی برای کد جدید خطرناک‌اند. آگاهانه تصمیم گرفته شد خواندن سخت‌گیرانه بماند تا خطا بلند شود و دادهٔ Legacy بی‌صدا normalize نشود. build ۰ خطا / ۱۶ warning پیش‌موجود (هیچ CS)، **۲۲/۲۲ تست سبز** (۱۲ قبلی + ۱۰ تست جدید قرارداد round-trip). commit توسط ارکستریتور پس از تأیید کاربر انجام می‌شود.
- 2026-08-18: **مسطح‌سازی محل Entityها و namespace جدید** (همان برنچ `feature/legacy-entity-guid-ids`، به درخواست صریح صاحب پروژه: «entityها باید مستقیم داخل Domain/Entity باشند»). کاربر از میان گزینه‌ها **مسطح‌سازی کامل + namespace جدید** را انتخاب کرد. هر ۶۵ فایل با `git mv` از `backend/src/Accounting.Domain/Legacy/Entities/` به `backend/src/Accounting.Domain/Entity/` منتقل شدند (پوشهٔ میانی `Entities/` حذف شد؛ git هر ۶۵ را به‌صورت rename تشخیص داد، پس تاریخچه حفظ شد) و `namespace Accounting.Domain.Legacy;` در همهٔ آن‌ها به `namespace Accounting.Domain.Entity;` تغییر کرد (BOM فایل‌ها دست‌نخورده ماند). پوشه‌های خالی `Legacy/` و `Legacy/Entities/` پاک شدند. تنها مصرف‌کنندهٔ بیرونی یعنی `Accounting.Infrastructure/Legacy/LegacyDbContext.cs` با تغییر یک خط `using` به‌روز شد. **سمت Infrastructure عمداً دست‌نخورده ماند** — پوشهٔ `Infrastructure/Legacy/`، کلاس `LegacyDbContext` و `GuidToChar36Converter` همان نام را دارند، چون آنجا واژهٔ Legacy هنوز درست است (نگاشت به schema قدیمی `CENTRALACCOUNT`). پیش از تغییر، ریسک تداخل نام بررسی شد: هیچ `using Accounting.Domain;` ساده، هیچ global using و هیچ شناسهٔ سادهٔ `Entity` در کد نبود، پس namespace جدید ابهام ایجاد نمی‌کند. `Accounting.Domain` همچنان صفر وابستگی خارجی دارد. build ۰ خطا / ۱۶ warning پیش‌موجود (هیچ CS)، **۲۲/۲۲ تست سبز**. commit طبق دستور ارکستریتور زده **نشد**.
- 2026-08-19: **فاز ۷ — حل دو ریسک 🔴 CRITICAL امنیتی + پورت `TokenManager` سازمان** (برنچ `GetAccountCode`). هر دو یافتهٔ CRITICAL که `security-reviewer` در Gate فاز ۶ ثبت کرده بود بسته شدند. **(۱) جعل‌پذیری `ADDUSERID`:** پارامتر `AddUserId` **کاملاً از `CreateAccountCodeCommand` و `CreateVoucherHeadCommand` (و Validatorهایشان) حذف شد** — نه صرفاً بی‌اعتماد شد؛ هر دو Handler حالا `ADDUSERID = _currentUser.UserId` می‌نویسند. چون پراپرتی دیگر اصلاً وجود ندارد، بازگشت این آسیب‌پذیری **خطای کامپایل** می‌دهد نه خطای منطقی (تأیید صریح `qa-tester`). این تنها تضمین ✅ باقی‌مانده در جدول Accounting Safety Gate (Audit Trail) بود که عملاً باطل شده بود. **(۲) نبود Authentication:** `ICurrentUser` در `Accounting.Application/Common/Interfaces/` (طبق الگوی `IUnitOfWork`، نه در Domain) + `HttpContextCurrentUser` در `Accounting.Api/Security/` بر پایهٔ claimها؛ `UserId` عمداً **throw می‌کند و هرگز truncate نمی‌کند** اگر کاربر احراز نشده باشد، claim نباشد، یا مقدار از ۱۰ کاراکتر (عرض ستون Legacy) بیشتر باشد — همان فلسفهٔ `ParseExact` در `GuidToChar36Converter`. اسکیم: **JWT Bearer محلی‌امضا، صریحاً موقت و مرحلهٔ توسعه** (انتخاب گزینهٔ B توسط صاحب پروژه) با fail-fast در startup برای کلید غایب/کوتاه‌تر از ۳۲ بایت، `SetFallbackPolicy(RequireAuthenticatedUser)` به‌جای `[Authorize]` پراکنده، و `POST /api/dev-auth/token` که **خارج از Development مقدار 404 می‌دهد** و به‌جای `TB_PERSON_ACTION` از allowlist در config می‌خواند. **خلأ مهمی که `security-reviewer` کشف کرد:** `app.UseAuthentication()` اصلاً در pipeline نبود — بدون آن `HttpContext.User` هرگز پر نمی‌شد و policy بی‌صدا بی‌اثر می‌ماند؛ اضافه شد **قبل از** `UseAuthorization()`. 401/403 چون استثنا نیستند هرگز به `GlobalExceptionHandler` نمی‌رسند، پس صریحاً با `JwtBearerEvents` به `application/problem+json` با `traceId` درآمدند. **یافتهٔ تعیین‌کنندهٔ schema:** جست‌وجوی هر ۶۵ Entity نشان داد **هیچ ستون password/hash/salt/token/credential در کل schema Legacy وجود ندارد** → schema Legacy اصلاً قادر به احراز هویت نیست و ساخت اسکیم جدید اجتناب‌ناپذیر بود؛ `TB_PERSON_ACTION` (تنها جدول با `USERID`+`OPERATORROLE`+`VAHEDCODE`) یک registry مجوز/اپراتور است نه credential store، و چون بررسی Read-Only زنده‌اش ناتمام ماند، عمداً به آن اعتماد **نشد**. **(۳) پورت `TokenManager`:** صاحب پروژه الگوی `IDP.Services.TokenManager` پروژهٔ اصلی خودش را فرستاد؛ به `Accounting.Infrastructure/Idp/` پورت شد با **۶ نقص اصلاح‌شده** (خطای بلعیده‌شده + `Console.WriteLine` → `ILogger` و انتشار `TokenAcquisitionException` به‌جای برگرداندن توکن کهنه/`null`؛ `new HttpClient()` در هر refresh → `IHttpClientFactory` واقعی؛ `static SemaphoreSlim` روی singleton → فیلد نمونه؛ `Dictionary` با read محافظت‌نشده → `ConcurrentDictionary`؛ `DateTime.Now` → `UtcNow`؛ تضمین عدم افشای `ClientSecret`/`access_token` در لاگ). `Resources` عمداً همچنان ارسال **نمی‌شود** (سؤال باز، حدس زده نشد). `ITokenManager` عمداً داخلی به Infrastructure ماند (هیچ مصرف‌کنندهٔ Application ندارد). اعتبارسنجی config **تنبل** است نه startup، تا نبود `Idp:*` بالاآمدن API را خراب نکند. **نکتهٔ معماری که مستند شد:** این `TokenManager` یک acquirer **outbound** است و به‌تنهایی CRITICAL #1 (محافظت inbound) را حل نمی‌کند. **`Program.cs` عمداً به IDP واقعی وصل نشد** و ۵ سؤال باز برای صاحب پروژه ثبت شد (token endpoint یا discovery URL؟ audience ورودی؟ کدام claim ≤۱۰ کاراکتر است؟ آیا inbound اصلاً باید به این IDP وصل شود؟ آیا `resource` لازم است؟) — طبق توصیهٔ `security-reviewer` حدس زده نشد، چون یکی‌گرفتن audience خروجی و ورودی یعنی پذیرفتن توکن‌هایی که برای سرویس دیگری صادر شده‌اند. build ۰ خطا، هیچ warning نوع CS، **۱۷۱/۱۷۱ تست سبز** (۲۲ Domain + ۸۴ Application + ۴۵ Api + ۲۰ Infrastructure جدید)، همگی Unit/Mock — **هیچ فراخوان شبکه‌ای به IDP و هیچ عملیات نوشتنی روی Oracle زنده انجام نشد**. تصمیمات باز عمداً حل‌نشده و ثبت‌شده: اعمال سمت‌سروری `VahedCode`، IDOR روی `GetById`، محدودکردن Kestrel به localhost، نبود تست pipeline احراز هویت (`WebApplicationFactory` عمداً ساخته نشد چون ممکن بود ناخواسته به Oracle زنده وصل شود)، و نبود allowlist برای `Roles` در `DevAuthController`. commit/push طبق دستور صریح کاربر انجام **نشد**.
- 2026-08-19 (ادامهٔ همان جلسه): **جایگزینی کامل JWT محلی با IDP واقعی سازمان.** صاحب پروژه پکیج رسمی `Tamin.Framework.Common.Security` **1.0.9** را از feed داخلی (`https://nexus.tamin.ir/repository/nuget-v2-group/`، ثبت‌شده در `backend/NuGet.Config`) در اختیار گذاشت. **پیش از هر تغییری، API واقعی پکیج با reflection و اجرای واقعی راستی‌آزمایی شد (نه حدس)** و دو یافتهٔ تعیین‌کننده به دست آمد: (۱) `AddTaminJWTToken` اسکیم استاندارد `Bearer` را با `JwtBearerHandler` ثبت و `DefaultScheme` را ست می‌کند، پس `SetFallbackPolicy(RequireAuthenticatedUser)` واقعاً scheme دارد و **نباید `AddAuthentication(...)` دوم اضافه شود**؛ (۲) پکیج خودش `JwtBearerEvents.OnMessageReceived` و `OnAuthenticationFailed` را wire می‌کند، پس نوشتن `options.Events = new JwtBearerEvents{...}` آن‌ها را **بی‌صدا نابود می‌کرد** — به‌همین‌دلیل shaping خطای 401/403 با `Configure<JwtBearerOptions>` و به‌صورت **زنجیره‌ای (chain)** پیاده شد: delegate اصلی اول `await` می‌شود و فقط اگر `!Response.HasStarted && !context.Handled` بود ProblemDetails ما نوشته می‌شود. (اصلاح یک برداشت اولیهٔ خودم: پراپرتی‌های `JwtBearerEvents` در ASP.NET Core به‌صورت پیش‌فرض non-null اند، پس «هر پنج delegate ست شده» مثبت کاذب بود؛ در عمل فقط دو تای بالا واقعاً assign می‌شوند — `OnChallenge`/`OnForbidden` روی no-op پیش‌فرض می‌مانند. chain کردن باز هم الگوی درست و forward-safe است.) پیکربندی: `validAudience` از `Tamin:Idp:Audience` با fallback به مقدار پیش‌فرض — ⚠️ این مقدار **به‌صراحت درخواست صاحب پروژه فعلاً با پروژهٔ `Financial_Account` مشترک است** تا audience اختصاصی این سرویس در IDP ثبت شود؛ `environment` هم مشروط `IsProduction() ? Production : Test`. رفتار تأییدشدهٔ پکیج: `ValidIssuer = http://idm.tamin.ir` با یک `JsonWebKey` **ثابتِ جاسازی‌شده** — **هیچ Authority/discovery/JWKS fetch ای انجام نمی‌شود**؛ همهٔ فلگ‌های `Validate*` روشن‌اند. **حذف فیزیکی زیرساخت محلیِ بلااستفاده** (طبق قانون پروژه، نه `[Obsolete]`): `DevAuthController` + تست‌هایش، `JwtOptions`، `DevAuthOptions`، و بخش‌های `Jwt`/`DevAuth` از `appsettings.json` (جایش `Tamin:Idp:Audience` خالی اضافه شد) — دیگر هیچ توکن محلی‌امضایی در پروژه صادر نمی‌شود. `ICurrentUser`/`HttpContextCurrentUser` عمداً دست‌نخورده ماندند. تست جدید `TaminJwtWiringTests` (۵ تست) اضافه شد که رفتار پکیج شخص‌ثالث را قفل می‌کند: ثبت اسکیم `Bearer`، resolve شدن default authenticate/challenge به `Bearer` (چیزی که کل کارکرد FallbackPolicy به آن وابسته است و یک ارتقای پکیج می‌تواند بی‌صدا خرابش کند)، اعتبارسنجی واقعی audience پیکربندی‌شده، و روشن‌بودن `ValidateIssuer`/`ValidateLifetime`/`ValidateIssuerSigningKey`. **جست‌وجوی پکیج `IDP` روی feed سازمان نتیجه‌ای نداد** → آن کد یک پروژهٔ لوکال داخل solution اصلی کاربر بوده نه پکیج منتشرشده، پس پورت دستی `TokenManager` به `Accounting.Infrastructure/Idp/` تصمیم درستی بوده و دست‌نخورده ماند. build ۰ خطا، بدون warning نوع CS، **۱۷۰/۱۷۰ تست سبز** (۲۲ Domain + ۸۴ Application + ۲۰ Infrastructure + ۴۴ Api). همهٔ تست‌ها Unit/Mock — **هیچ فراخوان شبکه‌ای به IDP و هیچ دسترسی به Oracle زنده انجام نشد**. **تصمیم باز 🔴 جدید که عمداً حدس زده نشد:** معلوم نیست کدام claim توکن واقعی IDP یک شناسهٔ **≤۱۰ کاراکتری** قابل‌نگاشت به `ADDUSERID` دارد (اگر `sub` یک GUID باشد جا نمی‌شود)، و نگاشت بین کدهای کاربری قدیمی Legacy و هویت‌های IDP جدید نامشخص است؛ ضمناً پکیج `NameClaimType` را روی `.../claims/name` ست می‌کند نه `nameidentifier`. این هرگز با توکن واقعی تست نشده — ولی چون `ICurrentUser.UserId` عمداً **fail-loud** است (throw به‌جای truncate)، هر ناسازگاری بلافاصله خطا می‌دهد نه خرابی خاموش دادهٔ Audit. commit/push طبق دستور صریح کاربر انجام **نشد**.
