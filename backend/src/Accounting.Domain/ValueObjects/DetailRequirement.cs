namespace Accounting.Domain.ValueObjects;

/// <summary>
/// حالت الزام یک نوع تفصیلیِ لینک‌شده به یک معین (قانون ۳).
/// تصمیم طراحی: این enum عمداً مقدار «Forbidden/غیرمجاز» ندارد؛ چون «غیرمجاز بودن»
/// به‌صورت ضمنی از طریق «عدم وجود لینک» (SubsidiaryDetailTypeLink) بیان می‌شود.
/// یعنی: لینک‌نشده = غیرمجاز. برای غیرمجاز کردن یک نوع تفصیلی که قبلاً مجاز بوده،
/// باید لینک آن با SubsidiaryAccount.UnlinkDetailType حذف شود، نه این‌که مقدار این enum
/// به «Forbidden» تغییر کند. هر دو مسیر (enum صریح یا عدم وجود لینک) در نهایت باید به یک
/// رفتار برسند؛ این‌جا مسیر «عدم وجود لینک» انتخاب شده چون ساده‌تر، بدون حالت زائد و
/// از نظر داده‌ای فشرده‌تر است (رجوع به chart-of-accounts.md).
///
/// <para>
/// <b>منسوخ (۲۰۲۶-۰۸-۱۷):</b> طبق تصمیم آگاهانهٔ کاربر، مدل نوشتن معتبر پروژه اکنون
/// Entityهای <c>Accounting.Domain.Legacy</c> (schema واقعی Oracle) هستند، نه این مدل Rich.
/// این تایپ صرفاً برای مستندسازی اجرایی (تست‌های موجود) نگه داشته شده و نباید در کد جدید
/// استفاده شود.
/// </para>
/// </summary>
[Obsolete("طبق تصمیم کاربر در ۲۰۲۶-۰۸-۱۷، مدل نوشتن معتبر پروژه Entityهای Accounting.Domain.Legacy است؛ این تایپ منسوخ است و برای کد جدید استفاده نشود.")]
public enum DetailRequirement
{
    Optional = 1,
    Required = 2
}
