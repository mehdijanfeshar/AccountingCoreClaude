namespace Accounting.Domain.ValueObjects;

/// <summary>
/// وضعیت سند حسابداری — نگاشت مقدار عددی ستون <c>TB_VOUCHERSHEAD.DOCLIFE</c> (که تا ۲۰۲۶-۰۹-۲۰
/// به‌اشتباه <c>bool?</c> اسکفولد شده بود؛ رجوع به ریسک باز #۲ در <c>CLAUDE.md</c>).
///
/// مقادیر مستقیماً از <c>Entities\Vouchers\DocLife.cs</c> پروژهٔ مرجع خوانده شده‌اند و
/// <c>docs/centralaccount-business-reference.md</c> §۳-۴ عیناً نقلشان کرده — حدس زده نشده‌اند.
///
/// <b>این enum ترتیبی است، نه صرفاً برچسبی.</b> گزارش‌های پروژهٔ مرجع با <c>&gt;=</c> فیلتر
/// می‌کنند نه <c>=</c> (یعنی «حداقل این درجه از قطعیت» — §۲۴ همان سند)، و همین تنها دلیلی است که
/// <c>bool</c> بودنش صرفاً «ناکافی» نبود بلکه مفهوم را اصلاً غیرقابل‌بیان می‌کرد.
///
/// ⚠️⚠️ <b>مقدار <c>0</c> عمداً در این enum نیست، ولی ستون Oracle ما <c>DEFAULT 0</c> دارد.</b>
/// یعنی احتمال دارد ردیف‌های زنده‌ای با <c>DOCLIFE = 0</c> وجود داشته باشند که <b>خواندنشان سالم
/// است ولی ویرایششان از طریق API با ۴۰۰ مسدود می‌شود</b> (چون Validatorها <c>IsInEnum()</c>
/// دارند) — دقیقاً هم‌الگوی ردیف <c>TYPEACTION = 5</c> که فاز ۲۵ روی دادهٔ زنده پیدا کرد. این
/// کوئری تأییدی اجرا نشده و به‌عنوان مورد باز در <c>docs/open-decisions.md</c> ثبت است. مقدار
/// <c>0</c> عمداً به enum اضافه <b>نشد</b>: معنایی برایش شناخته نیست و ساختن یکی، جعل قاعدهٔ
/// کسب‌وکار است.
///
/// ⚠️ ریسک باز جداگانه‌ای که این تغییر <b>حل نمی‌کند</b>: پروژهٔ مرجع تغییر وضعیت سند را از
/// ویرایش عادی <b>جدا</b> کرده (<c>ChangeStateCommandHandler</c>؛ <c>UpdateVouchersHeadCommand</c>
/// آن اصلاً <c>DOCLIFE</c> را لمس نمی‌کند)، ولی <c>UpdateVoucherHeadCommand</c> ما همچنان اجازه
/// می‌دهد این فیلد آزادانه از بدنهٔ درخواست عوض شود — ریسک 🔴 «سند Post شده واقعاً قابل تغییر
/// است». این تغییر فقط <b>نوع دادهٔ</b> غلط را اصلاح می‌کند، نه عملیات نامتمایز را.
/// </summary>
public enum DocLife
{
    /// <summary>یادداشت.</summary>
    Draft = 1,

    /// <summary>موقت.</summary>
    Temporary = 2,

    /// <summary>بررسی‌شده.</summary>
    Reviewed = 3,

    /// <summary>تأیید دائم.</summary>
    Accepted = 4,
}
