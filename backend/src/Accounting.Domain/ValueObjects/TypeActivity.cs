namespace Accounting.Domain.ValueObjects;

/// <summary>
/// ماهیت بدهکار/بستانکار حساب (سطح گروه یا معین) — نگاشت مقدار عددی ستون
/// <c>TB_ACCOUNTCODE.TYPEACTIVITY</c> (فعلاً به‌اشتباه <c>bool?</c>، رجوع به ریسک باز #۲ در
/// <c>CLAUDE.md</c>) به این enum.
///
/// معادل enum <c>TypeActivity</c> در پروژهٔ مرجع
/// (<c>D:\CentralAccount\Tamin.Core\Entities\AccountCodes\TypeActivity.cs</c>).
///
/// ⚠️ **دو نکتهٔ حیاتی (رجوع به <c>docs/centralaccount-business-reference.md</c> §۱-۴ و §۲۳-۱):**
///
/// (الف) این enum **۷ مقدار دارد، نه ۳**. کامنت ستون Oracle فقط به سه مقدار اشاره می‌کند و
/// گمراه‌کننده است.
///
/// (ب) کامنت موجود ستون Oracle («(1بستانکار2بدهکار3بد-بس)نوع فعاليت») **نادرست/کهنه** است و
/// مقادیر ۱ و ۲ را جابه‌جا گزارش می‌کند. مقدار واقعی، تأییدشده روی دادهٔ زندهٔ
/// <c>CENTRALACCOUNT</c> (نه حدس): <c>TYPEACTIVITY = 1</c> یعنی **بدهکار**
/// (<see cref="Debit"/>)، نه بستانکار. شاهد قطعی‌کننده: حساب‌های سطح گروه با نام‌های بدیهی
/// حسابداری («دارايي هاي جاري»، «هزينه ها» با مقدار ۱؛ «بدهي هاي جاري»، «بدهي هاي غيرجاري» با
/// مقدار ۲) دقیقاً با enum پروژهٔ مرجع هم‌راستایند، نه با کامنت Oracle. هیچ ویرایشگر آینده نباید
/// این مقادیر را بر اساس کامنت ستون Oracle «تصحیح» کند.
///
/// **نکتهٔ پیاده‌سازی‌نشده (فقط یادداشت):** پروژهٔ مرجع یک محدودهٔ وابسته به سطح اعمال می‌کند —
/// حساب‌های سطح گروه فقط ۱..۳ را می‌پذیرند (<c>AddGroupCodeValidator.cs:26-29</c>)، درحالی‌که
/// معین‌ها ۱..۷ را مجاز می‌دانند (رجوع به §۲۴-۱۱ همان سند). دادهٔ زندهٔ Legacy خودِ
/// ما همین‌الان این قاعده را نقض می‌کند — ۳ حساب سطح گروه با <c>TYPEACTIVITY ∈ {4,5,6}</c> وجود
/// دارند (ریسک باز #۱۳ در <c>CLAUDE.md</c>). به همین دلیل هر Validator آینده باید کل بازهٔ
/// ۱..۷ را برای همهٔ سطوح بپذیرد؛ محدودسازی وابسته به سطح عمداً اینجا enforce نشده است.
/// </summary>
public enum TypeActivity
{
    /// <summary>بدهکار.</summary>
    Debit = 1,

    /// <summary>بستانکار.</summary>
    Credit = 2,

    /// <summary>بدهکار-بستانکار (دوطرفه).</summary>
    DebitCredit = 3,

    /// <summary>بدهکار-طی دوره.</summary>
    DebitPer = 4,

    /// <summary>بستانکار-طی دوره.</summary>
    CreditPer = 5,

    /// <summary>بدهکار-پایان دوره.</summary>
    DebitFin = 6,

    /// <summary>بستانکار-پایان دوره.</summary>
    CreditFin = 7,
}
